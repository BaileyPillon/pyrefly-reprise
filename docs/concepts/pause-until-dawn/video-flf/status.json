{
  "tool": "tools/gen/video-flf.mjs",
  "model": "wan2.1_flf2v_720p_14B_fp8_e4m3fn.safetensors",
  "plate": "public/art/pause/yuna-ffx2.png",
  "note": "Pipeline built and verified against this ComfyUI's live /object_info (schemas saved in object-info/*.json). The graph was confirmed executing exactly as designed (checked via ComfyUI's own /queue listing mid-run). idle-breathing/seed1 was queued and left rendering for 2443s+ (40+ minutes) without error or completion by the end of this session -- this 14B fp8 model, on a 16GB card that had under ~1GB free VRAM before the job started, needs heavy automatic CPU offloading, so it is far slower per clip than round 1's 5B model (452-1024s/clip). It was left running detached (nohup) in the background at D:/Tools/pyrefly-video/flf/idle-breathing/1/ and D:/Tools/pyrefly-video/flf/idle-breathing-1.log; a follow-up pass should check whether it finished, then run `node tools/gen/video-flf.mjs join-report idle-breathing 1` and `contact-sheet idle-breathing 1` to score it. No clip in this pass reached a confirmed, scored completion, so nothing is picked or rejected yet -- this differs from round 1 in that nothing here failed the join/identity check, generation simply did not finish within this session's budget. At this per-clip cost, the full 9-clip x however-many-seeds brief is realistically many hours of wall time on this shared GPU, not one pass -- the same conclusion round 1's own README reached for its slower cases.",
  "gpuBeforeFirstQueue": "15084 MiB / 16303 MiB used, 98% util (i.e. under 1.2GB headroom before the FLF job was queued)",
  "clips": {
    "idle-breathing": {
      "1": {
        "verdict": "NOT CONFIRMED COMPLETE",
        "wallSecondsAtSessionEnd": 2443,
        "note": "Queued cleanly (queue was empty), executed the exact graph documented in README.md (confirmed via /queue), no errors in 2443s+ of log output, GPU stayed healthy (99-100% util, 13.5-15.4GB/16.3GB used, no OOM). Left running in the background past the end of this session."
      }
    }
  },
  "notRendered": [
    "idle-blinks",
    "smile-and-relax",
    "determined-and-relax",
    "hurt-and-relax",
    "turn-left-and-back",
    "turn-right-and-back",
    "look-up-and-back",
    "hair-breeze"
  ]
}
