# Art method r3 — Bailey's decision sheets

Chapter 6 (Leblanc, Logos, Ormi), **FFX-2 only**. Built per
`docs/plans/art-method-r3/METHOD-CHECK.md` §4, from files already on disk plus the
pilot and its judge — nothing was rendered, installed, or written to
`approved-hashes.json`. Sources: `gen_crops.py` documents every crop/composite and
where its source file lives; the game-size panels reuse a real in-battle capture
(`D:/Tools/pyrefly-lora/leblanc/r3/ingame/r22-idle-frame.png`) as the background.

## decision-1-paintings.png
**Question for Bailey: how many paintings should each boss get — A (fewest,
recommended), B (+ attack paintings), C (engine only), or D (+ rig motion for
hurt)?** Leblanc's cast ships as `cast.r2.2` ("6, repair failed") because the
independent judge scored the pilot transplant's choker at 6 and the kill rule fired;
Logos and Ormi are unaffected (each cast independently scored 7).

## decision-2-exit.png
**Question for Bailey: how should Leblanc, Logos and Ormi leave the fight — Today
(pyrefly dissolve, wrong for living humans), Yields (stays standing, dims, steps
back — ships as the `'yields'` departure kind), or Painted KO (a lying painting,
costliest and contradicts the research for four of eight headline opponents)?**

## decision-3-anchors.png
**Question for Bailey: lock these three idles (yes/no each), and settle the three
hard-rule-6 questions the chosen method will copy verbatim into everything derived
from them — Leblanc's fan colour (idle: closed and black; cast: open and warm
magenta), Ormi's shield heart (idle has none; the r1 cast does), and Logos's disc
(unsourced; idle and installed cast already agree).**

Reaction goes into the tile's `reaction` (liked / disliked / must remain / must
change / undecided) once Bailey answers; nothing here is built until he does.
