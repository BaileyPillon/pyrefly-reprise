# NOW — where Pyrefly Reprise stands

> **Snapshot: 2026-09-23 09:55 EDT (machine clock)**, written by the driver session ("FFX/FFX-2 2.5D game recreation").
> **USAGE MODE: NORMAL again at 11:25 EDT 09-23, Bailey's words: "Ok continue working pretty please" and "Continue on!", with ultracode switched on** (the driver had tightened to conserve at 09:45; this returns to Bailey's own 09-22 setting). Reading 11:20 EDT: weekly all-models 57 percent used, weekly Fable 24, 5-hour 17; the week resets 2026-09-26 07:00 EDT. Burn this morning was about 2.7 points an hour: at that pace protect (80 used) comes around 20:00 EDT tonight, so size every fan-out and re-read before each one.
> **BAILEY, 2026-09-23 about 12:20 EDT, verbatim: "Ok I want you to finish the next chapter end to end. I have the usage. Let's do it."** => the driver reads "the next chapter" as CHAPTER VIII EVRAE (its idles already pass the judges; Chapter VII's Seymour and Guardian idles do not), told Bailey so, and runs it in two parts: part 1 now (art, music, finish plan), part 2 after the release-10 batch frees src/engine, src/scenes and tactics (code fixes, full real-input playthrough, the approval package), then the unlock and release 10. Usage: Bailey says he has it: NORMAL, full depth. Bailey also asked for Wait-line drafts (sent about 12:15 EDT: 1 "Bar's full, she's up! Take your time, nobody moves while you're picking." (recommended), 2 "Her bar's full, so it's her turn. Relax, the whole fight waits while you choose!", 3 "Full bar means she's up! Everything holds still while you pick. Handy, huh?"); his pick is pending.
> **BAILEY, about 12:40 EDT, verbatim: "Wait line 1, and yes Evrae falls out of the sky"** => the FFX-2 Wait gauge coach line is APPROVED: "Bar's full, she's up! Take your time, nobody moves while you're picking." (being wired + recorded by a Sonnet agent); Evrae's exit is DECIDED: it falls out of the sky, no pyrefly dissolve (FFX only; built in Chapter VIII part 2). **Then: "We need to go FULL steam ahead"** and **"also i didnt see the updated pause menu can you show what the living portrait looks like now? I just need one example"** => sent `docs/concepts/pause-until-dawn/prototype-v2/shots/v4/living-portrait-v4.mp4` (the v4.1 clip as H.264 for the phone), captioned honestly: its own test page, not in the pause yet, the swap visible around 10 s.
> **BAILEY, about 12:45 EDT, verbatim: "Evrae widget A with C's staging, Anima A then B"** => both picks equal what is built (they were INFERRED): Chapter VIII's order widget and Chapter VII's arrival are now Bailey-named; the recording agent (item 24) moves them to named reactions in targets.json and decisions.json. Chapter VIII now waits only on its art, music, the fall-from-the-sky exit and part 2's fixes; Chapter VII on its idles and its finish plan.
> **BAILEY, about 12:44 EDT, verbatim: "Usage is no concern right now. Full steam ahead please."** => NORMAL at full depth, parallel tracks limited only by file ownership and the one GPU. Sent 12:46 EDT: the two Macalania battle-cue mood sketches (A "The Courtesy", B "The Processional") as MP3s for his pick.
> **BAILEY, about 13:25 EDT, verbatim: "All recommendations on the three art decisions."** => Chapter VI art: (1) option A, one hero cast per boss, hurt = idle under the flinch, no attack painting; (2) YIELDS: the Syndicate stay standing, dim and step back instead of dissolving (presenter departure kind, built with Evrae's fall after the release-10 presenter group); (3) lock the Leblanc, Logos, Ormi idles; fan red leaf with silver ribs (as the cast shows); Ormi's idle gains the Syndicate heart from his r1 cast; Logos's disc as is. Applied by item 31. He also asked for the auto-refreshing render viewer: http://127.0.0.1:8890/ ("latest renders", meta refresh 20 s; this PC only).
> **09:50 EDT 09-23: RELEASE 09 IS LIVE.** `main 5ddfde3`, bundle `Jw5R1yWQ`, deployed 07:55 EDT from `D:/pyrefly-release` under Bailey's owner override, verbatim: **"Work on pushing at least a live build with one new chapter"** (records in `e9b9bed`: deploys.log, `critic/pending/5ddfde3.json` owing live + focused + deep, the deep carried from fd0ae96 / 8f48237 / 1b33971). Contents: Chapter VI Leblanc UNLOCKED (pose art = best candidates, disclosed), FFX-2 ATB Wait mode DEFAULT with Active as the pause option + one-time save migration, the ATB SPEED pause row, the round-08/09 fixes (build gate, advisor card, music fades, intent panel, advisor committed items, per-enemy heights), chapters 7 and 8 registered but LOCKED. Deep review of the candidate (`critic/rounds/round-09.md`): ship HOLD / changed area FAIL on PR-0122 (the intent panel paints over the pause screen, a regression vs 1b33971), shipped anyway on the override; 105 issues, no critical. Live check (`critic/reviews/e9b9bed-live.md`): artifact 47/47 byte-identical PASS, real-input smoke to party prep PASS, 0 console errors, BUT the live agent saw the FFX-2 battle HUD stay `hidden` in chapters 4 and 6 (LIVE-HUD-HIDDEN, reported UNVERIFIED: debug-API path, possibly a GPU-less browser); the driver's real-input + GPU re-check REFUTED it at 10:00 EDT (item 15 below): the menu appears at 8.7 s. Release 09 was ANNOUNCED to Bailey at 10:10 EDT.
> **USAGE MODE: NORMAL, set by Bailey's word** (2026-09-21 20:00 EDT, offered the spend order "1 release tonight, 2 living
> portrait round 2, 3 music audition, 4 art consistency pilot; chapters after the weekly reset", Bailey answered:
> **"1+2 and 1 new chapter finished. Don't hold back."**). **NORMAL, set by Bailey's word, 2026-09-22 13:46 EDT, verbatim: "My usage just reset! Let's get serious and keep working. Awesome. Let's do it."** Weekly all-models 0 percent used, weekly Fable 0, 5-hour 0 (the week resets 2026-09-26 07:00 EDT). Five tracks run in parallel today (list below); the ComfyUI queue is shared by three of them, so a 60 to 90 minute video job may sit in front of a render: poll and wait, never restart ComfyUI while the queue has a job. Open with Bailey: his AUDIO score of the live build (settles the deep debt), the segmentation-model download for the portrait rig, and the Leblanc letters (art 1A/1B, ATB 2A/2B). Bailey sets the mode by word and agents may only tighten it (hard rule 15): the three
> tracks below run at full depth; anything else still waits.
> **BAILEY'S ANSWERS, 2026-09-22 21:45 EDT, verbatim, to the five-item list (1 chapter 5 at human speed A/B/C/D; 2 the ATB SPEED pause row; 3 audio score; 4 music round-1 scores + may model audio ship; 5 downloads SAM 2 / kohya + OpenPose / hall IR): "1. C Wait mode. Also I want the default to be wait mode instead of active mode please. 2. Please clarify what you mean 3. I'm at work so can't grade that yet 4. Ok 5. Yes".** So: D-009 (Active only, no Wait toggle) is SUPERSEDED: FFX-2 gets a WAIT mode (the clock stops while a command menu is open) as the DEFAULT, with Active as the other setting (build in flight, see the running list); the ATB SPEED row awaits his answer after a clarification; the audio score and music scores wait until he is home; "4. Ok" is NOT read as permission to ship model-generated audio (ask again with the scores); the downloads are approved and running (`D:/Tools/video-models/_dl/fetch-tools-2026-09-22.ps1`: SAM 2.1 hiera-small into D:/Tools/sam2 + ComfyUI's python, kohya sd-scripts venv on Python 3.11 at D:/Tools/sd-scripts, xinsir ControlNet OpenPose SDXL into ComfyUI/models/controlnet, Voxengo IRs into D:/Tools/audio-libs/ir; marker fetch-tools-2026-09-22.DONE.txt).
> **BAILEY, 2026-09-22 22:05 EDT, verbatim:** "2. Wait is now default but with active as an option. It's fine if it's faithful. 4. Where/how can I hear it? 5. Let me know when it's done please" => the ATB SPEED pause row is APPROVED (it is FFX-2's own Config setting, sourced); Wait default + Active option confirmed; the music round-1 excerpts go to him as phone-playable files; the downloads are reported when their marker lands.
> **BAILEY, 2026-09-22 23:35 EDT, verbatim: "please get to work on finishing the new chapter we cant delay any longer"** => Chapter 6 (Leblanc) SHIPS in release 09, UNLOCKED, with its pose art as the best candidates on disk (disclosed in the announcement); the identity-LoRA re-render of Leblanc / Ormi / Logos runs in parallel and lands in the next release. The driver's earlier default (hold the chapter until the poses pass) is overruled by his word. Release 09 cuts NOW from main HEAD (Macalania registered and locked; Evrae partly landed, its registration follows in release 10).
> **BAILEY, 2026-09-23 00:00 EDT, verbatim, on the three Wait-mode follow-ups: "1, 2, 3 I'll take your recommendations on all please"** => (1) a one-time save migration to Wait: ADOPTED; (2) the faithful top-level/submenu split: DEFERRED to release 10 (keep the whole-menu hold now); (3) the briefing's fourth line becomes mode-aware: his approved line when Active, a new Wait line recorded INFERRED. Release 09's cut (`wf_6117b73a-3d0`) was STOPPED at 00:02 EDT so the migration goes into the candidate (it is the save-data class anyway); `pyrefly-wait-mode-followups` builds it now; release.js relaunches when it lands.
> **BAILEY, 2026-09-23 00:20 EDT, verbatim: "Work on pushing at least a live build with one new chapter"** => release 09 cuts NOW from main HEAD (with or without the migration, whichever HEAD holds), runs the deep review of the candidate (save-data class), and DEPLOYS with these words as the owner override of the deep-owed cap (release.js `wf_` id in the running list; the deploy agent is authorised by these words; they settle no obligation). The migration and the LoRA art follow in release 10 if they miss the cut.
> **00:35 EDT 09-23:** the migration + mode-aware briefing line landed and were verified clean (87c82c6; Wait line "In hers, the clock holds while you choose." recorded INFERRED on tile C1, awaiting Bailey) but AFTER the release 09 cut pinned `e119552` (full suite 248 files / 5655 green at the cut; plan: deep review of the candidate before deploy). So release 09 ships WITHOUT the migration (existing saves keep Active until the X-2 BATTLE row is flipped once: disclosed) and release 10 carries the migration, the LoRA art and Evrae.
> **03:06 EDT 09-23: RELEASE 09 CANDIDATE e119552 HELD by its deep review (critic/rounds/round-09.md): PR-0085 CRITICAL, the production build FAILS (`npx vite build` exits 1: lightningcss "Invalid empty selector" from a nested `/* 14 */` inside a comment at src/ui/ffx/ffx-hud.css:819-822, commit d030d82), so no artifact exists to push, override or not; PR-0091 MAJOR REGRESSION, the advisor card sits on the party HP rows after a chain seam in chapters 5/6. Disclosed majors worth fixing before shipping: PR-0089 music fades read ms as s (boss themes near -80 dB, the select waltz plays through fights: pre-existing, live too), PR-0090 the enemy-intent panel never appears (CoachedHud lacks setIntentSource), PR-0086 Leblanc's White Wind heals nothing (immunity eats her own heal), PR-0088 the advisor double-spends items, PR-0093 the Syndicate staged at boss scale, PR-0046 the coach badge says the clock runs under Wait, PR-0087 Grenade damage unsourced, PR-0092 Dr Goon / Fem-Goon placeholders (art), PR-0096 Leblanc pose misses Bailey's pick B (LoRA art next), PR-0098 phone pause OPTIONS rows hidden but selectable. A repair workflow (`pyrefly-release-09-repair`) fixes PR-0085, 0091, 0089, 0090, 0086/0087, 0088, 0093, 0046 with a production-build check in its verifier; then release 09 re-cuts (deep-before again) and deploys under Bailey's override words.
> **04:15 EDT 09-23: the release 09 repair LANDED** (57524be + f461f2a build fix and lightningcss gate; 0ff00f2 advisor card never over the party rows; 71aafa8 + a5241be music fades; 624895d intent panel; a149d13 + d142fa4 advisor never re-spends a committed item or buff; 9ba0b71 + 5ddfde3 Syndicate at party scale; 87b6277 mode-aware badge; White Wind / Grenade in the Leblanc data commits; verifier then repair pass green; runner patched dccd36e). **Release 09 RE-CUT now** (release.js, Bailey's override words; item 10 is about the running agents). LoRA art so far: Leblanc poses judged 6/6/5/6 and Logos 6/6/5/5 (better than round 3, still under 7; Ormi pending): release 10.
> **RUNNING-LINE RULE (learned tonight):** when a workflow id below is listed as running, that line is ABOUT the agents of
> that workflow. If you are one of them, it is you: carry on with your brief. Nobody stopped for being "a duplicate".
> **DONE tonight: the pause remake is BUILT and VERIFIED** (`9af4e2c` target tile + D-021, `8cf1724` build, `921dfdb` fix
> pass after an adversarial pass with real input; `docs/handoff/pause-remake.md`). Owed: `tests/e2e/pause.spec.ts` still
> drives the old DOM; the plate does not follow the current dressphere; the FFX-2 grid-bonus bar is not built.
> **CHAPTER ART VERDICT (driver looked at the veto sheets 1:1):** painting quality is at Bailey's bar (Bailey on the Leblanc
> close-ups: "AMAZING ... exactly the kind of quality i expect") but the battle POSE SETS drift in identity (Leblanc's hair
> and dress change every state; Logos's helmet; Evrae's colours) because the monochrome guard in `4a41248` dropped `--ref`.
> The installed leblanc / ormi / logos / evrae sets are CANDIDATES, not approved: the `chapter:leblanc:2026-09-21` hash set
> is to be removed from `docs/target/approved-hashes.json`. Macalania art was not started.
> **TONIGHT'S THREE TRACKS:** (1) release 08 (pause remake + advisor v2 + Active ATB + onboarding on + restored art) after a
> tidy batch; (2) living portrait round 2 (study `wf_48a6ffff-8cc`, then the build); (3) the LEBLANC chapter finished end to
> end (art consistency pilot and re-render, scene, guide, registration, measured under Active ATB, verified) as release 09.
> **Art generation: ON** (Bailey, 2026-09-21: "Resume local art generation").
> **BAILEY'S DECISIONS TODAY, verbatim:** on the pause menu remake (Until Dawn character-screen layout in our art style,
> mockups in `docs/concepts/pause-until-dawn/`; asked grade A or B, may the text block move to the painting's empty
> side, are the meters right, may MUSIC be its own tab) — **"B, yes, yes, yes."** On scope: **"This needs to be
> included in our next build as well"**; **"The characters should be expressive and should be controllable by
> keyboard ... The characters need to be animated. In motion."** On release pacing: **"I'll go with your
> recommendation as usage becomes a concern. One chapter at a time is ok. I expect timely mockups for the pause menus
> please."** Build C scope, earlier the same day, still in force: **"you need to finish ALL chapters the new chapters
> need to be done"**; **"the move advisor needs to be way smarter and way more aware of what is going on turn by turn
> and what character you are controlling. music is too reminsicent of snes music instead of the more modern final
> fantasy titles and clair obscur. all of these next to be fixed very next build."**; on the music downloads and
> plan: **"yes you have my approvals there for music"**. A sub-agent that sees a status question relayed from the chat ("eta?") must NOT answer it: the driver
> answers the owner; agents do the task in their brief.

## Running now (2026-09-22 13:55 EDT; folder-owned; the RUNNING-LINE RULE applies: if one of these is you, carry on)

1. `wf_d471add8-24f` "pyrefly-release-09-batch" DONE 15:35 EDT, all on main: PR-0077 `d9f52d8`; FOC-05 `03adef3` + `d19d74f`; FOC-06 `2b49aa7` + `59f8d3c` (content loss at 1280x720 disclosed: the card sheds 3 rows to fit the bigger type); PR-0078 `a202d16` + `e2cd095`; PR-0075 `9512099` (preflight `docs/plans/ffx2-item-accuracy-review.md`); PR-0076/PR-0080 `15385ab` (a chain-locked girl keeps her menu and her held command; the menu owner never changes under the thumb), the sourced Config ATB speed lever `27c3601`, the pause ATB SPEED row `030d7e1` (**touches `src/app/SaveData.ts`: save-data class, so release 09 needs a DEEP review of the candidate BEFORE deploy**; the row itself still needs Bailey's yes, rule 9), engine split `349712f`, measurement `fafe6c9` + repair `9a4d029` (`docs/handoff/release-09-repair.md`). **PR-0076 is NOT cleared and is Bailey's call:** chapter 5 at Normal wins 40/40 at 0 s, 32/40 at 0.5 s, 27/40 at 0.75 s, 11/40 at 1 s, 5/40 at 1.5 s, 0/40 at 4 s per decision; Slow 5/40 at 1.5 s; chapter 4 40/40 everywhere; chapter 6 9/40 at 1.5 s. Options: accept as the last-fight spike; Slow; a Wait mode (contradicts D-009); a boss-side change (never without his word). Was: PR-0077 (`src/app/screens/pause/markup.ts`), FOC-05 (`src/ui/coach/**`),
   FOC-06 (the `.mad__*` stylesheet), PR-0078 (results screen files), PR-0075 (`src/battle/ffx2/**` item accuracy, with a
   preflight doc), PR-0076/PR-0080 + the sourced Config ATB speed lever (`src/battle/ffx2/**`, `BattlePresenterActive.ts`,
   pause OPTIONS row, preflight `docs/plans/ffx2-active-menu-review.md`), then `critic/bench/ffx2-active/**` measurement,
   an adversarial verifier and one repair pass. No deploy inside.
2. `wf_84d8b42e-3be` "pyrefly-music-modern-sound" DONE 15:15 EDT (b789047 sketch A: sfizz + VSCO/VCSL renderer, performance model, convolution hall; 6e8a144 sketch B: ACE-Step restyle at denoise 0.40; 667904b sketch C + the 'Modern sound, round 1' section at the top of `docs/audio/audition.html`: today / A / B / C per cue, level-matched, with a scoring row that copies a summary line). AWAITING BAILEY'S EAR. Open questions for him: may model-generated audio ship (all of B, C's layers); a real concert-hall IR download (plan A2). It OWNED `tools/audio/**`, `src/audio/**` (additive),
   `public/audio/candidates/**`, `docs/audio/**`, `docs/handoff/music-modern-sound.md`. Ends in an A/B/C audition on
   `docs/audio/audition.html` for Bailey's ear; no shipped cue changes.
3. `wf_76c1ca8d-eb7` "pyrefly-chapter-art-round" DONE 16:40 EDT. Method check `docs/plans/leblanc-art-method-check.md` (94bde34) found three root causes: the identity text described a costume the idle does not wear; IP-Adapter's CLIP-Vision CENTRE-SQUARE CROP only ever saw the mid-body of the tall unpadded idle (never face, hair, fan); and the framing words fought the poses. Method F (square-padded idle + head crop as a batched reference, idle-truth words) is now the best recipe and still falls short: independent judges score Leblanc attack/hurt/cast/ko 4 (39dc940), Logos 3 to 5 (a490609), Ormi 4 to 5 (d3460bf), Seymour 4 to 6 / Guardian 6 / Anima hurt 7 / backdrop 7 (d172ce4, judge c8f3714), Evrae redo teal and composed by sketch img2img (c152aff, judge pending). **Hard rule 15 STOP: no further word or seed pass; Bailey sees the best next to the idle and picks the next method.** The driver's proposal: a per-character LoRA trained on the approved idle and close-ups (identity) plus ControlNet OpenPose skeletons drawn from our own puppets (pose); both need downloads and Bailey's yes. The cleanup landed (aff3fa8: Ormi and Seymour facing sidecars proven live, white slabs and holes cleared, Seymour ko / Logos attack / Guardian ko edges padded, skin and colour unification for Logos, the Guardian and Ormi ko; originals backed up under D:/Tools/pyrefly-art-backup/candidates/2026-09-22-cleanup/). Evrae independent judge (3d410ed): idle-near 8, idle-far 7 PASS; breath-charge 6, hurt 6, ko 5, card 6; two cleanup fills on ko were real fin gaps (a regression, being undone by a follow-up agent with the fringe, coil hue and pose-scale metadata fixes). Was: method check `docs/plans/leblanc-art-method-check.md` (rule 15), pilot2,
   Leblanc/Ormi/Logos failing states, then Macalania (never painted), then the Evrae redo. OWNS those subjects under
   `public/art/**`, `docs/concepts/chapters/**`, `tools/gen/cast.json` rows. Everything CANDIDATE; no approved-hash entries.
4. `wf_01f0974a-1e1` "pyrefly-video-flf-round3" DONE on paper (e8abc6a fix, judge 17b9af2: correct on paper, no render: the queue never went idle). The driver's background job started the render at 16:49 EDT after ten idle minutes (`node tools/gen/video-flf.mjs render idle-blinks --seed 1`, log D:/Tools/pyrefly-video/flf/round3-idle-blinks.log); the clip rendered (97 frames, 73 min); independent judge d8770f0: FAIL (geometry pinned on every frame, TONE not: a washed grade from frame 2, blown-out frames 94-97; a slow double blink). Rule 15: second failed FLF render on the join. THE FIX FOUND IN POST (5ed1f92, `video-flf/round3/pingpong.md`): per-frame affine colour match fitted on the pinned body/background (frame-1 vs frame-1 error falls below the VAE floor) + PING-PONG playback (forward to the still frame T=81, then reversed) so every cut lands on the plate frame; the colour-matched hard cut reads as the same shot at 1:1 (demo-20s-cm.mp4, 33 s, shown to Bailey). Rule for the clip set: every clip's motion must be finished and at rest well before its last frame; blinks and gaze belong to the rig (it passes the timing spec); video supplies breathing, hair and subtle life. Next: a written method note, then ONE more render at 81 frames with a 1-frame end anchor (same seed A/B) to see whether the grade wander is the 4-frame batch. OWNS `tools/gen/video-flf.mjs`, `tools/gen/join_report.py`, `docs/concepts/pause-until-dawn/video-flf/**`,
   `D:/Tools/pyrefly-video/flf/**`. Its ComfyUI jobs take 60 to 90 minutes each.
5. `wf_cc2af145-0bf` v3 and `wf_39f6741a-f84` v4 "pyrefly-living-portrait" DONE 07:30 EDT 09-23. v3 (0236347 3f0cf66 6caf0ef c3dc63d 44d39a2 c4eacfc d93caa5): one painting at a time, rest = plate to the pixel. v4 (7 agents): Yuna identity LoRA 164da53 (step 2000 reproduces the plate), keys every 20 degrees a09e4a1, SAM 2.1 masks + warp f0515cf 2bb99aa 08d238e fc1169d, check REFUTED (mixed paintings, hair change at +70..85, tears), v4.1 fix 8dfefc6 4f40a18 559519d: one painting at a time with a 0.2 s matched change, mirrored wide keys, painted lids. NOW: held on any key it reads as ONE Yuna (tips, heterochromia, tassel, line); remaining: the +40 to +60 fringe dissolve (+60 is a mirror), the tassel fading at -50, an invented tassel disc at -60/-85 (Bailey decides), no expressions on the wide keys. shots/v4/{sheet.jpg, clip-15s.webm}; docs/handoff/living-portrait-v4.md. NEXT: Bailey's look, then the port into src/app/screens/pause/PortraitStage.ts (mount / setGaze / blink / setExpression / dispose).
6. `wf_4486692a-5b3` "pyrefly-video-flf-round4" DONE 23:25 EDT (643457f the post tool `tools/gen/video-post.mjs`: per-frame colour match + auto turnaround + ping-pong + join report; 9693e66 the A/B: the 4-frame RepeatImageBatch end anchor CAUSES the blow-out, a 1-frame anchor ends flat near the VAE floor; judge 4818878: FAIL on MOTION, not joins: with a restrained prompt the model produces no measurable breathing (chest 0.02 px over 81 frames), the second such failure). **VIDEO ROUTE PARKED** (rule 15: a method check is owed before a third breathing render): the joins are solved (endBatch 1 + colour match + ping-pong) but the model either drifts or sits still at this size and cost (73 min a clip). The rig (item 5, round 4 running) is the living-portrait path; revisit video only for a stronger-motion clip (hair in a breeze) if the rig cannot give hair life. OWNS `tools/gen/video-flf.mjs`, `tools/gen/video-post.*`, `docs/concepts/pause-until-dawn/video-flf/round4/**`.
7. `wf_cc2af145-0bf` (item 5) and `wf_4486692a-5b3` were interrupted by a session restart at ~18:50 EDT and RESUMED at 21:00 EDT with the same run ids; their interrupted agents re-run and find their own uncommitted work in the tree (prototype-v2/art/v3, prototype-v2/src, tools/gen/inpaint.mjs, rig-turns.py, video-flf.mjs).
8. `wf_552781c6-d19` "pyrefly-chapters-macalania-evrae" DONE 01:15 EDT 09-23 (14 agents): CHAPTER 7 Macalania registered and LOCKED (27ed39e guide/meta, 823450d scene + Anima's arrival to the driver's recommendation (tile inferred), 62b4927 registration, 08fae83 measure: intended line 36/40, advisor 39/40; verifier CRITICAL: Anima never appeared in a real battle and shattered Guardians stayed on stage -> d968a93 fixed both; art faces the party). CHAPTER 8 Evrae registered and LOCKED (d030d82 guide/meta + order widget option A, 84dccf9 foredeck scene + NEAR/FAR switch, dcb9861 registration, e119552 measure: intended 39/40, advisor top row 0/40 -> a2fb6d0 fix: the card's top row now wins, Inhale telegraph shown, AND A SHARED PAUSE BUG FIXED: with the pause up, mouse clicks reached the battle HUD underneath (present in release 08 and in the release 09 candidate e119552: disclosed; fixed for release 10)). Open: Bailey's picks for Anima's arrival and the Evrae widget (both built to recommendation, inferred); the chapters' own music cues (chapter 1's cue stands in); three reward items with no ItemDef row (Q2, needs a yes); Seymour spriteKey and heroArt path mismatches for chapters 6/7 (a small fix agent is on them with the four-drop results overflow); the title DOM leaks under a battle reached by the debug gotoChapter after a real chapter select. Was: (launched 21:05 EDT): Macalania then Evrae registered end to end like Leblanc (c473de8), both LOCKED as Coming until Bailey approves their art; Anima's arrival and the Evrae order widget built to the driver's recommendations (A then B's tag; A + C's staging) and recorded as INFERRED in docs/target/targets.json for Bailey's yes. OWNS src/scenes/{macalania,evrae}*, their data/guide/tactics/meta files, src/story registration, additive entries in encounters.ts + registries (docs/CONTRACT-CHANGES.md), chapterGrid card data, docs/handoff/chapter-{macalania,evrae}*.md.

9. `wf_0f0e2938-66e` "pyrefly-ffx2-wait-mode" DONE 23:20 EDT: a93a4f4 preflight + decision D-029 (D-009 superseded, Bailey's words verbatim), e1844cd engine + settings (Wait default; the X-2 BATTLE row read by the engine; applied at chapter start and on pause close), 34c9bb4 measurement (in Wait, chapters 4, 5 and 6 win 40/40 at 0, 1.5 and 4 s per decision, byte-identical logs; Active control unchanged), verifier refuted four points, repair 3e25962 (the HUD chip now tells the true mode; a pause flip reaches the open menu; no X-2 BATTLE row in FFX; and a LIVE-BUILD bug found on the way: the coach wrapper dropped the optional FFX-2 clock methods, so under Active the pump never moved the real HUD's gauges). Full suite 242 files / 5560 green. `docs/handoff/ffx2-wait-mode.md`. OPEN FOR BAILEY: (a) existing saves keep Active until the row is flipped once (a one-time migration is a save-data change: yes/no); (b) the research's faithful split (clock runs at the top-level command window, stops in submenus) is NOT built: the build holds the clock for the whole menu, which is more forgiving: keep or build the faithful split; (c) the approved briefing line "In hers, the clock does not wait" is false under Wait: A keep / B show only when the row reads ACTIVE / C reword. Release 09 is save-data class (deep review of the candidate before deploy). Was: (launched 21:55 EDT): Bailey's decision built: FFX-2 WAIT mode (clock stops while a menu is open) as the DEFAULT, Active as the other setting; preflight `docs/plans/ffx2-wait-mode-review.md`, the decision record superseding D-009, the existing X-2 BATTLE ACTIVE/WAIT pause row wired, measurement, verifier, repair. OWNS `src/battle/ffx2/**` (this feature), `BattlePresenterActive.ts`, pause options/settings, `SaveData.ts` (one optional field), `BattleScreenWiring.ts`, `docs/target/decisions.json`, `docs/handoff/ffx2-wait-mode*.md`.

10. release.js ROUND 09 (first launch `wf_1339a90c-ae8` at 23:45 EDT stopped ITSELF at the cut because it read this line and could not tell it was the run described; relaunched at 23:47 EDT as `wf_6117b73a-3d0`, stopped 00:02 EDT for the migration, relaunched 00:18 EDT as `wf_67ebd565-334` WITH Bailey's override words, which stopped on a stale preview server left by the 00:02 stop; relaunched 00:20 EDT as `wf_80bf0f45-845`, whose deep review HELD e119552; repaired and RE-CUT 04:15 EDT as `wf_313554e4-39b`, same override words). **If you are the cut, review, deploy or status agent of a release.js run, THIS LINE IS ABOUT YOU: carry on. There is no other release process; D:/pyrefly-release holds no live process (stale ones were stopped by the driver).** The run: cut from main HEAD, deep review of the candidate BEFORE deploy (save-data class), then the deploy (refused on the deep-owed cap unless Bailey overrides), live verification, status. Contents: Chapter 6 unlocked with its candidate art (disclosed), Wait mode default, ATB SPEED row, the round-08 fixes, Macalania registered but locked.
11. `wf_e091d1ef-a2e` "pyrefly-leblanc-art-lora" DONE 05:05 EDT 09-23 (12 agents): identity LoRAs trained and picked at step 1500 for leblanc (2024d07), ormi (7e2fef1) and logos (in D:/Tools/pyrefly-lora and ComfyUI/models/loras/<name>-x2.safetensors; the LoRA reproduces each idle at 1:1); poses with LoRA + OpenPose skeletons installed as CANDIDATES (leblanc a6386b2 + redo 1d87d8f; logos d455885 + repaints d80b99e; ormi ebdec18 + redo 3f201fb). Independent judges: Leblanc 6/6/5/6, Logos 6/6/5/5, Ormi 5/7/4/4 (up from 4-5; cast passes; the redos are self-judged 6-7 and unjudged). Remaining defects are local (boots, a fan leaf colour, one missing leg, glossy style on Ormi hurt/ko, Logos idle's hanging-gun defect learned by the LoRA). Not checked in a running battle (scale). NEXT (release 10/11): LoRA round 2 with the best outputs added to the datasets, the Logos idle hand repaired first, an in-game scale check, an independent judge. Rule-6 question for Bailey: Leblanc's open fan colour (research: red and silver; the idle never shows it open; the installed cast uses red and silver). Was: (launched 23:45 EDT): identity LoRA per subject (leblanc, ormi, logos) with kohya, poses by OpenPose skeletons, judged, installed as candidates for release 10. OWNS public/art/characters/{leblanc,ormi,logos}/**, docs/concepts/chapters/leblanc/lora/**, D:/Tools/pyrefly-lora/{leblanc,ormi,logos}. GPU rule: LoRA training waits for an empty ComfyUI queue and under 4 GB used (the rig's LoRA may be training).

12. `wf_1c478604-662` "pyrefly-leblanc-art-lora-round2" DONE 09:40 EDT 09-23 (6 agents, 4.5 h): LoRA r2 for leblanc (c2f0559), ormi (fee1121), logos (81bc5e5 + c3fbcfe + 84a5cd5) on grown datasets, re-rendered with OpenPose, scale matched in battle, installed as CANDIDATES in `public/art/characters/{leblanc,ormi,logos}/`, backups in `D:/Tools/pyrefly-art-backup/candidates/2026-09-23-lora-r2/`. INDEPENDENT JUDGES (0c46fc2, c71b595, 5ed7a7a): NO SUBJECT AT THE BAR. Leblanc 6/6/6/5 (obi drift is the new defect), Logos cast PASSES at 7 (attack 6, hurt 6, ko 5), Ormi 6/5/5/4. Ormi's round-1 cast (independent 7) was REPLACED by a self-scored 6 for set coherence: restorable from `.../lora/ormi/round2/replaced/`, Bailey's call. Two in-battle findings for the code side: the prone KO plane is centred on the slot (Leblanc's head lies behind Logos) and the state lighting blooms her dress and hair near white (same as PR-0097 on Yuna). RULE 15: two rounds left the same issues open => a written method check before any round 3, and CONSERVE mode forbids art agents anyway. Art is PAUSED.


13. `wf_313554e4-39b` release.js ROUND 09 DONE 13:36 EDT (24 agents): deploy 5ddfde3, live check, and the DEEP REVIEW OF THE LIVE BUILD = `critic/rounds/round-10.{json,md}`: deployment PASS, changed area FAIL, ship HOLD (for the next candidate). The deep obligation (critic/pending/5ddfde3.json, carried from fd0ae96 / 8f48237 / 1b33971) did NOT settle: required-not-tested CHK-B1 (Bailey's audio verdict), CHK-022 chapter 3 real-input win (live has the Doublecast self-cast), CHK-024 same-origin save upgrade. An Opus agent (13:37 EDT) runs CHK-022 without Doublecast and CHK-024 on live and appends a round-10 addendum, then re-runs critic-clear. CHK-B1 = Bailey. Until the deep settles, release 10 needs Bailey's override words (third deploy while a deep is owed). NEW majors in round 10: PR-0148 (the shipped score is the one Bailey called SNES-like; D-026 "fixed very next build"), PR-0142 (FFX: after a mouse click on the PAUSE chip, Enter re-opens the pause), PR-0143 (= the Steal bug, being fixed in item 30), PR-0144 (ch6 guide keys attack hints on a boss being present, not on the target), PR-0150 (FFX-2 single-target targeting far from its approved tile, party framed out). SENT 13:38 EDT: the four FFX battle-theme clips (control / A sampled orchestra + hall / B AI restyle / C mix) asking Bailey for the modern-sound pick and a 1-10 score of the live audio.
14. Release-10 fix batch DONE 10:45 EDT 09-23, all on main, validated by the driver (tsc clean, 176 tests in the 8 touched files green, full suite run before the push, no new orphan): c01742b PR-0125 FFX Doublecast asks for a spell and an enemy target, engine safety net, permanent probe test, real-key chapter 3 WIN with 26 Doublecasts and 0 self-casts (FFX only; adds optional `AvailableCommand.wrapsCategory`, CONTRACT-CHANGES entry); fee2033 PR-0126 the advisor card keeps its "in <submenu>" chip on every rung but the phone one (shared plumbing, so both; chapter 5 replay 202 decisions, 0 missing); 8b2ff51 PR-0124 carried HP/MP clamped to the dressphere a seam lands in (FFX-2 only; the dressphere carry itself stays with Bailey); 59b348a PR-0122 the intent panel and chip hide under the pause and come back in their prior state (both games; six real-key combinations at 1600x900 and 1280x960 pass elementFromPoint), PR-0123 the headline shows the rolled move's own odds ("Most likely" only when it is the top branch, "Possible" below 50 percent) and both games' odds rows sum to 100. Proof: `docs/screenshots/fix10/`. `node tools/critic-plan.mjs` for 59b348a: DEEP after deploy, FOCUSED before (shared combat core, both engines, global layout). SEEN IN THE PROOF SHOTS, not fixed: (a) the move-advisor card `.mad` has the same z-index escape over the pause (spawned task chip, `move-advisor.css:45`); (b) two floating MISS numerals sit over the paused screen in intent-pause-ch6.png (the numerals layer also escapes the pause; probably on live too, check before calling it a regression); (c) that shot shows NO Yuna close-up on the YUNA tab (black left half): either the plate had not loaded in the headless capture or the plate does not follow Gunner (the pause-remake handoff already owes "the plate does not follow the current dressphere"); the focused review must look. Files over 400 lines grew (CommandMenu.ts 858, advisor.ts 1750, execute.ts 419, MoveAdvisor.ts 630): pre-existing, owed a split.
15. LIVE-HUD-HIDDEN REFUTED 10:00 EDT 09-23: a Sonnet agent drove the live URL with REAL KEYS ONLY in a GPU Chromium (ANGLE D3D11 on the RTX 5070 Ti, 1600x900, fresh profile): title > chapter select > Chapter VI > party prep > scene > the command menu was visible 8.7 s after the battle screen loaded, reproduced twice (`docs/screenshots/live-09/hud-ch6-menu-appeared.png`, the driver looked: ATTACK / SKILL / CHANGE / ITEM, party rows, enemy gauges, the intent panel, the Wait badge MENU'S UP GAUGES HOLDING). The live agent's observation was an artefact of a GPU-less debug-driven run. Lesson for CHECKS: a live smoke that cannot reach a command menu must first print its WebGL renderer string and frame rate. Two things the driver saw in that shot for release 10: the first-time coach quote ("Bar's full, she's up, don't wait for me, we all go at once!") reads as Active-mode coaching under Wait (check `coachCopy.ts`, FFX-2 only), and the intent panel's ODDS list is cut at the bottom (PR-0010 family).

16. `wf_37164f3c-400` "pyrefly-release10-batch2" (launched 11:25 EDT 09-23; 12 to 16 agents): five file-disjoint groups fix 17 round-09 majors, each group then gets an adversarial real-input verifier and at most one repair: command-menus (PR-0002, PR-0018, PR-0019, PR-0012; src/ui/ffx/**, FFX-2 command menu), presenter (PR-0022, the prone-KO placement, PR-0097, then PR-0005, PR-0031, PR-0094, PR-0061; src/engine/**, src/scenes/**), frontend-prep (PR-0127, PR-0063, PR-0065), intent-panel (PR-0010, PR-0011), advisor-noop (PR-0006; src/engine/tactics/**). Commits to main, no push; proof in docs/screenshots/fix10b/. If this line is about you: carry on.
17. Pause cleanliness DONE 11:55 EDT (driver looked at `docs/screenshots/fix10/pause-clean-ch6.png`: no card, numerals or intent panel over the pause, Yuna's close-up renders): ef9f067 `.pause` z-index 999 + pointer-events auto (both games); 21f6270 the FFX-2 gauge coach line ("Bar's full, she's up, don't wait for me, we all go at once!") no longer shows under Wait, left unseen for a later switch to Active (FFX-2 only; NO Wait wording exists: a line for Bailey to supply or approve); the missing YUNA close-up was a headless load-timing artefact. The FADES ON ITS OWN / FIRST TIME ONLY chips are Bailey's approved onboarding legend. Host note: the machine ran at 90+ percent CPU with 100+ node/chrome processes; e2e runs flake on infrastructure under that load.
20. `wf_65934e60-02c` pause group DONE 13:55 EDT (fix 499b9d1, 13d4789, 1ff12d1; verifier REFUTED PR-0079 and PR-0098; repair 2c2de85, 00b8199, 93a2dab). DRIVER CHECK of the repair shots: PR-0098 FIXED (docs/screenshots/fix10b/pr0098-ffx2-options-scrolled.png: every row a real flex row, BATTLE HELP reachable at 390x844). PR-0016 BUILT on the approved "Hero plate, chapter 1/4" tiles ("The Until Dawn remake moves it to the CHAPTER tab"): accepted. PR-0079 STILL FAILS: at 1280x960 FFX-2 Yuna's IN THIS FIGHT values (WHITE MAGE / PROTECTION HALO) sit on her green eye in the repair's own proof (pr0079-ffx2-bahamut-yuna-1280x960.png). Two attempts failed => RULE 15: a written method check before a third (the options: pan the plate so the face clears the chrome as approved frame (c) does, narrow or stack the IN THIS FIGHT column, or per-size framing), in the next batch.

18. `wf_e9645b6e-d5d` art method check r3 DONE 11:40 EDT: `docs/plans/art-method-r3/METHOD-CHECK.md` (697a88e). Both judges ranked "change the question" first. The driver checked its two engine claims in the code: every chapter 6-8 enemy action is `kind: 'ability'` and `poseForCommand` (BattlePresenterEvents.ts:392) sends ability to CAST, so enemy ATTACK paintings are never drawn; an enemy KO is `dissolveTo` + `removeCombatant` (BattlePresenterBeats.ts:127-137), so enemy KO paintings are never drawn either. Method: one hero CAST per subject repaired with the idle's own pixels (transplant + seam-band repaint), hurt = idle under the flinch or a rig bake, no attack, no lying KO for the humans. Bailey decides: (1) A fewest paintings / B plus attack / C engine only / D rig hurt; (2) how the humans leave (today's dissolve / yields / painted KO); (3) lock the idles + fan colour, Ormi's heart, Logos's disc. GPU for all three chapters under A: about 4 to 5 hours.
19. `wf_28b5b729-099` Leblanc r3 pilot DONE 13:15 EDT (2bf3ead4 pilot, 707adab4 judge, fdc70185 decision sheets). Obi/knot/tassel transplant WORKS (CIEDE2000 1.17, judged 7); the 13 px choker FAILS (6: the Lab ring lock lightened it; core without the lock 2.13) so the kill rule fired; hurt bake dead (reads as a giggle, 5): hurt ships as the idle under the flinch. The driver authorised ONE last choker-only transplant without the ring lock (Opus agent, 13:20 EDT, cap 10 GPU min); final either way. SENT TO BAILEY 13:19 EDT: `docs/concepts/chapters/art-r3-decisions/decision-{1-paintings,2-exit,3-anchors}.png` with recommendations: 1 A (fewest paintings), 2 Yields (the Syndicate stay standing: research "Leblanc, Logos and Ormi make peace"), 3 lock the three idles, fan red leaf with silver ribs (as the cast already shows), Ormi's idle gains the research's "Syndicate heart logo", Logos's disc as is.

SENT TO BAILEY 11:33 EDT 09-23: the Evrae order-widget sheet and the Anima arrival sheet, asking for the two picks that unlock chapters VIII and VII (both built to the driver's recommendation and recorded INFERRED).

21. `wf_25360e74-11b` Chapter VIII part 1 DONE 14:10 EDT. ART (21fcc55 + repair): the deck plate IS concept B, byte-identical (judged 10); hurt derived from idle-near's own pixels (judged 7, kept); breath-charge failed at 5, repaired as an in-place throat swell from idle-near's pixels (installed CANDIDATE, 0 GPU; re-judge running); the cast candidate (head down and forward, jaws open; NOT installed) for Bailey's Decision 1 on Evrae; all backups in D:/Tools/pyrefly-art-backup/candidates/2026-09-23-evrae-r3/. MUSIC (QA pass, 8/8 in game): `scene-fahrenheit` "Within the Hour" and `boss-evrae` composed, rendered (-16 LUFS), wired into the chapter, the enemy and the script, on the audition page; the NEAR/FAR stem switch needs an engine hook (ask Bailey whether to build it). PLAN `docs/plans/chapter-evrae-finish.md` (e9dcad7). PART 2 OWES (after the release-10 presenter group): Evrae's fall from the sky (Bailey's yes) and the Syndicate yields (Bailey's yes) as presenter departure kinds; the scene grades B's daylight to dusk (src/scenes lights: match the picked concept); harmlessTurn Defend in the advisor line; the advisor card over the FAR streak; the party/victory rig framing NEAR Evrae under the CTB column (an options round first); the chapter name: D-020 adopted "the airship names the chapter" but the code ships "Evrae" (reconcile from research); then the unlock and a real-input win/loss.

22. `wf_a10d8dff-4e1` "pyrefly-ch7-and-goons-idles" (launched 12:42 EDT, 5 agents): Seymour and Guado Guardian production idles FROM Bailey's approved concepts (seymour-b, guardian-a) by the r3 method, Dr. Goon and Fem-Goon idle OPTIONS (3 each, Bailey picks), `docs/plans/chapter-macalania-finish.md`; an independent 1:1 judge. GPU queued behind the Leblanc pilot and Evrae art. If this line is about you: carry on.
23. `wf_c411abf6-c9b` paper checks DONE 13:05 EDT (b77ae3a PR-0008, PR-0007 doc, Wait-split preflight, portrait v5 method; review 55050554 confirms all four with corrections). PR-0008 and PR-0007 are NOT balance defects: Chapter 1's line is at a measured ceiling (100/160), the Zombie-to-Full-Life gap is faithful CTB math (both enemies Agility 38, ties to Seymour); the fix is teaching (guide rule 4 wrong, Holy Water line over-promises, an honest-odds rule, an intent line saying whether anyone acts before the mount) plus re-baselining the PR-0008 bar to 95/160: Bailey's wording picks first. Wait split measured: chapter 5 at 0.5 s on the top list 32/40, at 1 s 11/40, 1.5 s 5/40 (today's hold 40/40): Bailey A (faithful split) or B (keep the hold); build it switched off only if A. Portrait v5 = flow-grown keys every 10 degrees + hole-only LoRA repaint + hard cuts, -40..+40 first.
28. `wf_dfc14b42-290` (launched 13:10 EDT, 3 agents): the portrait v5 pilot (GPU about 10 min: pilot.mp4 + compare.mp4 vs v4.1) with an independent judge, and the Chapter 1 teaching options sheet for Bailey (`docs/concepts/chapter1-teaching/`). If this line is about you: carry on.
24. Sonnet agents: the Wait coach line + the two decision records (launched 12:38 EDT); the Vegnagun facts fix d57524a cherry-picked from branch claude/serene-haslett-e9c384 onto main (launched 12:42 EDT). If this is you: carry on.
25. Peer session "Investigate slow HUD restore after gotoChapter skipCutscenes" (Bailey started it): committed c24fe9d on branch claude/vibrant-raman-9f21b9 (docs/DEV.md + one JSDoc line in src/debug/api.ts: skipCutscenes skips only the story scripts; the battle opening follows speed; under 20 fps the whole game runs in slow motion because of the 50 ms frame cap). The driver cherry-picks it at the next cut. Its advice, adopted: do NOT tie the battle opening to skipCutscenes (it would strip the boss reveal from Retry).

26. `wf_621e8d28-92b` "pyrefly-portraits-and-vegnagun-parts" (launched 12:47 EDT, 3 agents): OPTIONS for the missing FFX-2 speaker portraits (Nooj, Brother; PR-0058) in the approved portrait style, and for Vegnagun's placeholder Bulwark / Redoubt / Node (PR-0095: derived from the approved painting, new, or off-screen); independent judge; install nothing. GPU queued. If this line is about you: carry on.
27. Research sweep DONE 13:15 EDT (622053a, `docs/plans/questions-for-bailey-2026-09-23.md`, research files updated). SETTLED by sources, to build next (after the golden re-record lands): Grenade base 200 with no guaranteed crit (drop `bonusCrit: 100`, re-measure ch6; PR-0087), five FFX sphere ItemDefs (the reward rows), FFX-2 names its own duplicate parts (Node A/B/C, Right/Left Bulwark, Right/Left Redoubt: rename, stop lettering FFX-2 enemies; PR-0013), Redoubt steal Phoenix Down x1 / rare Mega Phoenix x1, 50 percent, Pilfer 350 gil. LIVE BUG since release 09: Chapter 6 gives Rikku Steal and Pilfer Gil and both do nothing (Steal spends the turn; Pilfer shows a 0 hit, takes no gil): fix as a bug. FOR BAILEY: the dressphere carry at chain seams (no source: A carry, B revert and say so, C check a real copy) and Leblanc's open-fan colour ("red and silver" is one uncited wiki sentence; his picked concept shows lilac; rec. red leaf, silver ribs).

29. Golden logs re-recorded DONE 13:20 EDT (dd74444): after 697c038 exactly ONE event differed per chapter 5 run (the Leg victory's drop id), a sourced fix; the Charon test failure was a 15 s timeout under machine load (15.26 s), not a regression.
30. `wf_fdd32625-287` sourced data fixes: FFX items DONE and verified (spheres.ts + 0ca8293..4a6d605, ba3dfd8; results now print "Lv. 4 Key Sphere", "Ability Sphere x3" etc.; a report-only test lists FFX-2 reward ids with no row: gris-gris-bag, x2-mute-shock, x2-l-bomb, x2-budget-grenade, mythril-bangle, twist-headband, charm-bangle, reassembled-sphere). The FFX-2 engine agent DIED after committing a752322 (Steal and Pilfer Gil wired) and 3648ba6 (Grenade base 200, no crit), leaving the Redoubt steal uncommitted; `wf_4bacbce3-263` (launched 13:47 EDT) finishes it plus PR-0013 names/no lettering and adversarially verifies all four with one real-key Steal check. If this line is about you: carry on.

31. `wf_c597728e-dc8` Chapter VI art decisions APPLIED 14:15 EDT: D-034 (A), D-035 (Yields, not built), D-036 (anchors) recorded (2a6b818); option A installed (269ce89: Ormi cast = r1 cast.960106, sha f0a88fc5; the trio's attack/hurt/ko moved to backup, 18 files; the manifest lists only cast + idle; GPU run: 40 casts, 71 hits on idle, 0 /art/ errors); Ormi's idle gained the Syndicate heart from his cast's pixels (judged 8, 5df552f; the driver looked: it reads painted on at 1:1), installed (sha 521ca575); the three idles LOCKED in approved-hashes.json as chapter:leblanc-idles:2026-09-23 (96d55fa; verify-approved: 0 mismatched, 0 missing). Known, disclosed: Ormi's r1 cast carries a lavender shield while his idle's is red-rimmed (PR-0096 family).

**14:15 EDT: THE 5-HOUR LIMIT HIT** and killed nine agents mid-task (vegnagun-parts options, portraits judge, FFX-2 data verifier, the round-10 gap agent, the batch-2 presenter repair, the Evrae re-judge, the portrait v5 pilot, the goons options, the ch7 judge). Two other agents (Guado Guardian idle, Macalania finish plan) read Bailey's latest chat message as their task, skipped their briefs and SENT HIM portrait stills (duplicates): every prompt now says the chat is the orchestrator's and forbids sending files to the user. **Bailey, ~14:40 EDT: "Resume please my usage reset".** Reading 14:41: 5-hour 0, WEEKLY 80 PERCENT (all models; Fable 29), resets 2026-09-26 07:00 EDT. Resumed in priority order: `wf_64c8c99c-65f` (14:40, the critical path: presenter repairs PR-0097 FFX regression + PR-0061 vs cut-in, Chapter VIII part 2 = Evrae falls-away, Syndicate yields, lighting to concept B, HUD lettering + PR-0142 + FAR card, harmlessTurn + PR-0144, the chapter name; re-run of the FFX-2 data verifier; a full real-key Chapter VIII playthrough) and `wf_82e98feb-9c2` (14:42, the Evrae re-judge + the CHK-022/CHK-024 gap captures). HELD pending Bailey's word on the weekly allowance: Guardian idle, goons options, ch7 judge, Macalania plan, portrait v5 pilot, Vegnagun parts, Nooj/Brother judge. Batch 2 results (wf_37164f3c-400): FIXED PR-0063, PR-0065 (letter fallback removed), PR-0010, PR-0011, PR-0018, PR-0022, PR-0031, PR-0005, PR-0094, PR-0006 (guard existed; acceptance test 99921e5); PARTIAL PR-0127, PR-0019; LEFT FOR BAILEY PR-0002 (the 2-row cap cut the approved 5-row mock; reverted) and PR-0012 (the FFX-2 help slab collides with the party rows; switched off); REFUTED PR-0097 (FFX ch1 regression) and PR-0061: in the resume run. Seymour Macalania idle r3 INSTALLED CANDIDATE from concept B's pixels (935674f; human ear repaired per research 9.2; the old idle faced away because a cleanup set facing right; hurt.json may be mirrored too). Nooj/Brother options built (9af30a2), unjudged. Chapter 1 teaching sheet SENT to Bailey 14:43 EDT.

**21:00 EDT 09-23: RELEASE 11 LIVE CHECK PASS** (critic/reviews/d9decadb-live.md; live obligation settled PASS): 48/48 bytes, Chapter VIII real-key from the board (Pull back order), VII still COMING, two-tab save PASS, release-08 upgrade PASS, reload PASS, 0 errors, 0 404s; polish LIVE-D9-1 (P while paused via Esc does not resume). OWED on d9decadb: focused + deep (after the weekly reset 2026-09-26 07:00 EDT per Bailey; the deep settles only with his CHK-B1 audio score). WORK HELD at weekly ~85 percent until Bailey says otherwise. Queue for the next session, in order: (1) PR-0079 written method check (pause chrome over FFX-2 faces at 1280x960), (2) Chapter VII finish (Guardian idle from concept A, goons options, Seymour r3 judge, Macalania plan, its music mood pick A/B pending with Bailey), (3) portrait v5 pilot, (4) Vegnagun parts + Nooj/Brother judge (options built 9af30a2), (5) Chapter 1 teaching build after Bailey's wording picks (sheet sent 14:43 EDT), (6) the Wait split only if Bailey picks A (rec B), (7) the modern music after his A/B/C pick, (8) layout calls PR-0002 / PR-0012 / PR-0005-FFX-slab.

**20:25 EDT 09-23: RELEASE 11 IS LIVE, CHAPTER VIII UNLOCKED.** main `d9decadb`, bundle `B4_kxWCJ`, deployed from D:/pyrefly-release under --owner-override="I'll go with your recommendations let's get to work"; preflight at d9decadb: tsc clean, 289/289 files (6020 tests), build smoke, approved art 124 ok; live serves artifact 47e7bf045756f627 byte for byte (48 files). Records bbefe78 (pushed). Unlock `wf_5afde421-0d2`: 3847807 unlock (tests pin VIII unlocked, VII locked), 4ce8a94 D-037 title Evrae / D-038 art approved / D-039 cues accepted not by ear + OWNER-VERDICT, 0a61d54 approved-hashes set chapter:evrae:2026-09-23 (6 files; verify 124/0/0), d9decad independent verify PASS x4. Disclosed, not approved: characters/evrae/ko.png still shows in game; pause/evrae-chapter-card.png CANDIDATE. critic/pending/d9decadb.json owes live + focused + deep (deep after the weekly reset per Bailey). Live check running (critic/runner/live.js, 20:28 EDT) with the targeted save checks: two tabs + a release-08 save upgrade + the Chapter VIII smoke.

**BAILEY, ~19:50 EDT, verbatim: "I'll go with your recommendations let's get to work"** (after "1, 2, 3 help me decide what are your recommendations?") => Chapter VIII: (1) title stays "Evrae" (the airship keeps the location line, mechanic and music brief; supersedes D-020 for the title only); (2) the Evrae art APPROVED as shown (backdrop B, idle-near 8, idle-far 7, breath-charge 7, hurt 7; actions show the idle; the orange glow is our unsourced choice); (3) both cues ship, accepted on the recommendation, NOT by ear (CHK-B1 stays unverified), aftermath silence from beat 9; and RELEASE 11 ships on his word (the save-data class two-tab merge c784e553 would want a deep review before the deploy; the driver recommended shipping on his word with a targeted save check on live: two tabs + an old-save upgrade; the deep follows after the weekly reset). `wf_5afde421-0d2` (19:53 EDT): records + the Evrae approved-hash set + the unlock + an independent real-key verifier. Then the driver cuts release 11 by hand with --owner-override="I'll go with your recommendations let's get to work".

**19:05 EDT: CHAPTER VIII FIXES DONE** (`wf_c0ef36b0-a02`; verifier CONFIRMED F1 and F2 with real keys): a28f208 F1 (at FAR with a breath charged no row names Evrae; spare Potion / Eye Drops / Echo Screen; bench intended 40/40, advisor top row 40/40; one advisor-guard line both: a wrong-state miss counts as no effect), 80672bf F2 (music(null) at beat 9) + F4 (hold the Inhale frame 700 ms before Wakka's line), fb2858a fall shrinks to 0.22 + F5 (Cid's Sensor reveal no longer steals the enemy plate). Minor open: the new §4.5 hint text also shows on uncharged FAR turns (keyed on item labels); Yunalesca's aftermath also plays victory-ffx under it (same class as F2, FFX). Main verified by the driver: tsc clean, full suite 289 files / 6019 tests, pushed. Chapter VIII now waits ONLY on Bailey: the name, the art approval, the cues and the aftermath music (asked 18:10 EDT). Release 11 candidate = main HEAD (includes c784e553, the save-data class two-tab merge => deep review of the candidate before deploy, or Bailey's words).

**18:55 EDT: the "volume not persisted" live finding is a TWO-TAB SAVE RACE, not a release-10 regression**: one tab keeps Master Volume through reloads 6/6 on live; with a second game tab in a battle, its 5 s play-time write puts back its stale copy of the whole save (3/3 lost, also on 5ddfde3; whole-save writes since the store began, the 5 s write since 886722e 09-18); the pane review had several tabs open. FIXED in main c784e553 (both; save-data class => a DEEP review of the candidate before its deploy, or Bailey's override): SaveStore.save() merges when another tab wrote since this tab's last read (changed settings/flags from this tab, unlocks and coach marks unioned, attempts and play time summed, clears OR-ed, bests min), new src/app/saveMerge.ts, tests/unit/save-two-tabs.test.ts 10/10 (7 fail before), full suite 289 files / 6017 tests; real-key two-tab 3/3 kept. Not done: a background tab shows its old settings until its next write (up to 5 s in battle); a same-millisecond race remains.

**18:10 EDT: CHAPTER VIII STATUS** (`wf_64c8c99c-65f` done; e2e screenshots 7119762f in docs/concepts/chapters/evrae/e2e/): real-key WIN and LOSS, Retry, widget A (D-032), the six NEAR/FAR re-stagings, the fall (D-031), backdrop B byte-identical, both cues in place, results, 1280x960: PASS; 0 console errors, 0 404s. FAIL: F1 at FAR with a breath charged the advisor suggests Lancet -> Evrae (the trap); F2 victory-ffx plays under the whole aftermath (the script has music(null) at beat 9); F3 phone HUD at 390x844 (global); minors F4 a Wakka line over the breath charge, F5 a stray "I CID" sensor tag, Wakka off frame at 1280x960. Fixes running: `wf_c0ef36b0-a02` (18:08 EDT: F1, F2, F4, F5 and the fall shrinking as research says). SENT TO BAILEY 18:10 EDT with the art sheet and both cues: (1) the chapter name, "Fahrenheit" per D-020's adopted recommendation or "Evrae"; (2) approve the Evrae art set (actions show the idle: the cast candidate failed at 6); (3) the two cues by ear, and what plays under the aftermath (rec: the scripted silence). Presenter batch also done: PR-0097 fixed in both games (FFX-2-only figure mask 0.7), the cut-in no longer delays the menu (db7b832), departures module 314b61c, Evrae lighting to B 53d8660; PR-0005 FFX slab over the bottom-left menu for 0.76 s left for Bailey.

**18:00 EDT: RELEASE 10 LIVE CHECK** (critic/reviews/5348c2e3-live.md): artifact PASS (885 files), real-input smoke PASS (Steal took an X-Potion, the Syndicate stayed standing, the pause clean, Doublecast asked spell and target, prerendered music, 0 console errors), CHK-024 reload smoke FAIL: a Master Volume change in the pause applied but did not persist through reload (3/3, run in the in-app pane); the live obligation is settled FAIL. An Opus agent (18:01 EDT) reproduces it under Playwright real keys on live, bisects 5ddfde3..5348c2e3 (pause commits suspected), fixes with a test. If this is you: carry on.

**17:29 EDT 09-23: RELEASE 10 IS LIVE.** main `5348c2e3`, bundle `CcBx7HBL`, 582 art files, deployed under Bailey's override "Just push the build live please" (two earlier deploy attempts stopped before publishing: a flaky audio-manifest lock test under load (EPERM on a temp lock; 3/3 alone), then the dist-smoke folder left in the release worktree). Live serves artifact e8e31b1856ea405a byte for byte (74 files). Records bfeb3f5 (pushed). critic/pending/5348c2e3.json owes live + focused + deep (deep carried from fd0ae96, 8f48237, 1b33971, 5ddfde3). Live check running: critic/runner/live.js `wf_5689e335-2ee` (17:33 EDT). Focused and deep reviews NOT launched: weekly allowance 83 percent; the deep review last cost 5.3M tokens; Bailey to decide.

**BAILEY, ~17:15 EDT, verbatim: "Just push the build live please"** => RELEASE 10 cut by the driver by hand (release.js recipe): worktree re-added at main HEAD, npm ci, art mirrored (782 files), preflight RED on one test (tests/unit/chapters/evrae-advisor.test.ts: 0fa68f5 made the Evrae quiet turn aim Cheer at a capped member; bisected), fixed by the driver in 5348c2e (FFX only), re-cut at 5348c2e: tsc clean, 287/287 test files (6003 tests), build smoke exit 0, approved art 118 ok / 0 mismatched. critic-plan: FOCUSED before deploy (skipped under the override), live + DEEP after. main pushed at 5348c2e. DEPLOY RUNNING 17:27 EDT with --owner-override="Just push the build live please" (log D:/pyrefly-release/deploy-r10.log). The resume workflow `wf_64c8c99c-65f` finished every group (fixed + verified: PR-0097 FFX regression, PR-0061, Evrae falls-away D-031, Syndicate yields D-035, Evrae lighting to B, PR-0013 lettering, PR-0142, the FAR advisor card, Evrae harmlessTurn, PR-0144, the FFX-2 data fixes); its Chapter VIII end-to-end playthrough is still running.

**14:50 EDT: the round-10 gaps are CLOSED** (fe05e951): CHK-022 chapter 3 real-key WIN on live through all 7 links without Doublecast; CHK-024 save upgrade on live PASS (old 1b33971 save and the real release-08 profile: progress kept, one-time flip to Wait with the marker, Active sticks through reloads). critic-clear: live, focused and deep for 5ddfde3 are held ONLY by CHK-B1 = Bailey's 1-10 listening score of the live audio. New minor: a chained chapter's saved best-turns records only the last link. Evrae re-judge (5d952b4): breath-charge cycle 2 PASS at 7 (installed); the cast candidate FAILS at 6 (not installed; Evrae keeps its idle for actions until Decision 1 on Evrae).

**MACHINE LOAD, 13:41 EDT 09-23:** commit charge 96 of 103 GB (Windows grew the page file at 13:13: "Virtual Memory Minimum Too Low"), a headless Chrome died with 0xe0000008 (out of memory) at 13:39, about 15 agents plus Codex sessions (codex-runtimes node processes) run browsers and vite, ComfyUI holds 15.5 GB. The driver launches NOTHING new until several tracks finish; infrastructure failures in browser checks now are load, not product.
32. e2e type-check DONE (eebe252): `npm run typecheck:e2e` 66 errors to 0, one shared Window.__pyrefly type from src/debug/api.ts (tests only).
33. Opus gap agent (13:37 EDT): CHK-022 chapter 3 real-input win on live without Doublecast, CHK-024 same-origin save upgrade on live, a round-10 addendum and critic-clear. If this is you: carry on.

34. Leblanc cast p7 INSTALLED 14:05 EDT as the Chapter VI hero cast CANDIDATE (sha256 772661a5...; judge 60bc201: 6 overall, choker a high 6 at 2x (right-end cluster, dashed bottom edge), obi 7, no worse than r2.2 anywhere; in-game shot 2906651, 0 404s). The judge advises NO further choker repair: Leblanc's cast ships below the bar, disclosed; at game size the choker is about 13 px. r2.2 backed up.

ComfyUI (:8188) is shared by 2, 3, 5, 6, 11, 12: poll `/queue`, wait, queue one batch at a time, never restart it while a job runs.

## Live: release 09 (Build C.2)

- https://baileypillon.github.io/pyrefly-reprise/ = `main 5ddfde3`, bundle `Jw5R1yWQ`, deployed 2026-09-23 07:55 EDT under
  Bailey's owner override, verbatim: **"Work on pushing at least a live build with one new chapter"** (settles no review
  obligation). Contents and verdicts: see the 09:50 EDT header line. Obligations in `critic/pending/5ddfde3.json`: live
  (UNVERIFIED report `critic/reviews/e9b9bed-live.*`; the driver's re-check decides), focused, deep (carried). Disclosed
  majors this release: PR-0122 (intent panel over the pause, regression, fix in flight), PR-0123, PR-0126, PR-0124,
  PR-0125 (FFX Doublecast self-cast, fix in flight), PR-0096 candidate pose art, PR-0092 Dr Goon / Fem-Goon placeholders,
  PR-0098 phone pause rows, FOC-06, PR-0094 ch5 seam camera, PR-0097 bloom on Yuna, PR-0087 Grenade damage (awaiting
  Bailey), PR-0127 party-prep card clipping, plus the carried list in `critic/rounds/round-09.md`.

### Previous live build: C.1 (release 08), kept for the deep-08 debt that carries

- https://baileypillon.github.io/pyrefly-reprise/ = `main 1b33971`, bundle `BvhtVfzJ`, deployed 2026-09-21 23:48 EDT under
  Bailey's owner override, verbatim: **"Push the live build now please"** (the override authorised the deploy agent; it
  settles no review obligation). Contents: the pause remake on the Until Dawn character screen, move advisor v2, FFX-2
  Active ATB, Auron's briefing switched on, art round 4 restored, the front-end type floor, and FOC-01 to FOC-04 fixed.
  The build before it was B.1 (`8f48237`).
- Focused review of the candidate: SHIP, with disclosed majors FOC-05 (the FFX first-turn coach mark overlaps the advisor
  card, first time only) and FOC-06 (the advisor card's type sits under 12 px, inherited from the live build). live
  verification PASS (settled 2026-09-22 00:45 EDT, 863 files, real-input smoke, reload smoke, 0 console errors); deep
  review round 08 ran on the live build (critic/rounds/round-08.md): deployment PASS, changed area FAIL, ship verdict
  HOLD recorded against the NEXT candidate because of PR-0076 (chapter 5's intended line falls from 40/40 wins to 0/40
  at any human decision time because Active ATB runs the clock under an open menu; a regression against the previous
  live build) and PR-0080 (an open command menu is replaced in place by another character's list with no keypress, in
  262 ms, under the player's thumb); the critic's player-side correction: let a chain-locked character keep her queued
  command (four in five lost menus are chain locks), never tune a boss; disclosed majors PR-0077 (pause CHAPTER tab: 15
  hand-written art paths miss their art/ segment, 84 404s, one-line fix at src/app/screens/pause/markup.ts:130, do not
  prefix heroArtFallback), FOC-05, FOC-06, PR-0075 (FFX-2 healing items roll the enemy hit check against the ally's own
  Evasion and are consumed on a miss; FFX's accuracy.ts has the carve-out FFX-2 lacks), PR-0078 (victory results screen
  crops the leader's portrait to one eye with object-fit cover, both games); the deep obligation is STILL PENDING
  (carried from `fd0ae96` and `8f48237`): it needs Bailey's audio audition verdict (CHK-001/CHK-B1, third round asking),
  one real-input victory through to results per game (CHK-022), the advisor's on-screen checks CHK-004/005, the
  CHK-002/007/009 sweeps and a chapter 3 in-battle capture; score provisional, audio unscored, every category under 9,
  13 of 30 required targets matched.

## In flight: Build C (the next live build)

- Scope: earlier today's directive still stands — all three new chapters finished (Leblanc at the Chateau, Seymour and
  Anima at Macalania, Evrae on the airship), move advisor v2, modern-sounding music, FFX-2 Active ATB (built and
  verified: `45f98b9`, `bf19c37`, `33dba4d`, `073f678`) and onboarding switched on. Bailey has now also folded the pause
  menu remake into this build (see decisions above) and set the release pace to one chapter at a time as usage requires.
- Done: engine and data for Macalania (`074a198`, 80 percent) and Evrae (`dc1979f`, `959fade`, 97.5 percent); quick wins
  (`c7f5be4`, `71059ae`, `8fe4f99`, `de99ba4`); three story scripts; decision records (`26ee387`); music plan
  `docs/plans/music-modern-sound.md` (`fe11c60`).
- **Move advisor v2 is BUILT and measured** (`55540b9`, `f0ff68a`; `docs/handoff/advisor-v2.md`). Forty seeds a chapter,
  a bot pressing the card's top row every turn: ch1 62.5 to **67.5** percent (the chapter line itself wins 65), ch2 82.5
  to 85 (line 97.5), **ch3 0.0 to 97.5** (line 97.5), ch4 100, ch5 100 to 97.5. Chapter 3's median falls from 448 turns
  to 216. Latency p50 1.4-3.6 ms, p95 2.3-5.1 ms. Three defects, all found by running the engine: a switch's incoming
  member was not part of its command identity, so "put Lulu in" picked Wakka; the ownership gate refused Doublecast's aim
  at the boss, so ch3's line was thrown away on every Lulu turn; and a status the preview answered at its median was
  charged as a wasted turn, so ch3's Slow sorted behind Cheer. `MoveAdvisor.ts` untouched, nothing in `src/battle/**`
  changed. **Chapter 2 cannot reach its bar and the reason is measured, not tunable:** all 319 of its 319 divergences over
  eight seeds are the chapter line calling for Defend, which the FFX command window does not paint at all - so the
  auto-battler wins ch2 with a move no player can press. Bailey's call (handoff section 7).
- **DONE today: pause-remake options picked.** Bailey graded the Until Dawn options round "B, yes, yes, yes" — grade B,
  the text block may move to the painting's empty side, the meters are right, and MUSIC gets its own tab.
- **DONE today: living-portrait research and prototype** (`f1729e3`: `docs/plans/pause-living-portraits.md`,
  `docs/concepts/pause-until-dawn/prototype/`; run `npx vite --port 5477`, open
  `/docs/concepts/pause-until-dawn/prototype/`). Recommendation: procedural head-depth shader parallax plus blink and
  expression patches inpainted with our own Animagine model, zero downloads (rejects LivePortrait: its InsightFace
  buffalo_l dependency is non-commercial). GPU draw p50 0.036 ms. Awaits Bailey's verdict **by feel** on which reading
  of the gaze to keep (head, eyes or camera) before it is ported into the pause screen's PortraitStage; blink and the
  expression toggle are faked in the prototype and want inpainted patches; Wakka's tilted eye line and the one-eyed
  plates (Auron, Kimahri) are not handled yet. Optional download needing a yes: `depth_anything_v2_vits.pth`, 99.2 MB,
  Apache-2.0.
- **DONE today: the music downloads Bailey approved** ("yes you have my approvals there for music") are on disk under
  `D:/Tools/audio-libs` (vsco2-ce 3.2 GB, vcsl 6.2 GB, `_dl/sfizz-1.2.3-win64.zip`) and
  `D:/Tools/ComfyUI/ComfyUI/models/checkpoints/ace_step_v1_3.5b.safetensors` (7.7 GB); the VSCO 2 CE SFZ patches branch
  is cloning into `D:/Tools/audio-libs/vsco2-ce-sfz`. Checksums: `D:/Tools/audio-libs/_dl/fetch-music-2026-09-21.DONE.txt`.
  Still needing a per-item yes: hall impulse responses, any ACE-Step custom nodes.

## State of the three tracks (2026-09-22 02:40 EDT)

(a) Release 08 LIVE as above; next batch must lead with PR-0076/PR-0080 (or Bailey's word that the Active trade is what
he wants at this price) and PR-0077.

(b) LEBLANC = Chapter 6, registered end to end (`c473de8` registration, `68b8b6b` scene, `e226a5a` guide/meta, `5e22dfb`
data findings, `597e304` fixed a critical: the painted enemies never appeared because of an 'ffx2-' spriteKey prefix;
`8c646c4` measured under Active ATB: 40/40 at zero decision time, 4/40 at 1.5 s, 0/40 at 4 s, chapter 4 control 40/40
everywhere); ART for leblanc/ormi/logos re-rendered with the pilot's method A (forceRef 0.35 from the idle; pilot
`9e08955`; sets `9621f0e`, `1b33971`, `66e3e83`, `177396e`, `920dac8`, `2e5672d`) and STILL CANDIDATE, below the bar
(Leblanc attack reads as a flourish, hurt wears a different robe and does not read hurt after two attempts, Logos
headgear changes between states, Ormi shield/heart inconsistent); no approved-hash entries; three music cues owed
(chapter 4's reused), Paine art missing (stand-in), Act I/II dioramas reuse the Last Room. Waiting on Bailey: art 1A
ship as disclosed candidates / 1B hold; ATB 2A ship disclosed like chapter 5 / 2B build the sourced Config ATB speed
lever first; release 09 now or after the reset.

(c) LIVING PORTRAIT: rig v2 (`docs/concepts/pause-until-dawn/prototype-v2/`, commits `6000edf` keys, `572e18f` patches,
`63021ac` layers, `ebb71b0` runtime, `17b960e`, `98ff595` fix, `8d5c611` re-capture, handoff
`docs/handoff/living-portrait-v2-fix.md`): motion passes the measured spec; the assembly does not: box-edged layer
cut-ins, a collar seam in every still, a doubled iris and hair box edge at -40 degrees, patch rectangles; next round =
silhouette-true masks (segmentation model download to propose), hidden-region inpainting, per-triangle mesh warp,
colour-matched patches, profile-right by flip + iris swap, closed eyes and blink intermediates, brows via the fringe
layer. Video route (`docs/concepts/pause-until-dawn/video-preview/` round 1 on the 5B model: identity holds on subtle
motion, lost on turns, stitched preview REJECTED by Bailey; `video-flf/` round 2 on Wan 2.1 FLF2V 14B: identity 9, no
framing drift, joins 19x better, but the END frame is not anchored because WanFirstLastFrameToVideo masks a phantom
slot; fix = feed end_image as a 4-frame batch via RepeatImageBatch, motion peak earlier, 24 fps; ~72 min per clip; VAE
floor face MAD 3.85; no earring on this plate, the markers are the braid and clasp); rule: nothing with a visible join
is shown to Bailey.
- Next, in order: (1) pause remake verified, then the living portrait ported for the main party; (2) one tidy pass:
  advisor v2 bench harness fallback and three test defects (`docs/handoff/advisor-v2.md`), Leblanc provenance labels,
  front-end phone type floor FE-001; (3) first incremental release, round 08, launched **top-level** with
  `critic/runner/release.js` from the clean worktree: pause remake + advisor v2 + Active ATB + onboarding switched on +
  restored art + quick wins; (4) music workflow, after the 20:30 EDT reset and once the GPU is free: SFZ
  multi-velocity renderer, ACE-Step restyle, A/B/C audition on `docs/audio/audition.html` for Bailey's ear; (5)
  chapters one at a time (Leblanc, Macalania, Evrae): scenes, staging, guides, registration, release each.
- Open with Bailey: the gaze reading by feel (prototype: head, eyes or camera); Evrae order widget (rec. A + C's
  staging) and Anima arrival (rec. A then B's tag); Macalania cue mood A or B and the music audition; chapter 5 under
  Active ATB (0 of 40 wins at human decision times: measured options owed); Nul spells party-wide if sourced; the
  items already listed in "Waiting on Bailey" below that these facts do not close.

## Waiting on Bailey (none of these blocks the release)

Whether Active mode includes "an enemy hit closes the open menu and delays that character" (asked 2026-09-21, not yet
answered); the cost of a denied turn (marked `[estimate]`); whether FFX-2 letters a boss's same-named parts;
whether the advisor may overrule the guide's pinned pick when a revive scores far higher; a mute row in OPTIONS;
whether the never-wired "Turn cut-in" tile still stands; options rounds for the defeat screen, the advisor card, the
enemy-move panel and the phone layout; an audio score out of ten; Chapter 5 length and Chapter 1 difficulty (bring
measured options).

## Machine

- D: is flagged dirty and RAM runs XMP 6400 past AMD's 5200 limit: crashes and in-place file corruption. Boot-time
  chkdsk is scheduled for C: and D:. Bailey is turning XMP off in the BIOS, then updating the BIOS (FA2 to FC4c) and
  trying 6000. Guides: `C:\PyreflyBackup\BIOS-quick-guide.pdf` (2 pages), `PC-stability-guide.pdf` (full).
  After the repair: run `git fsck`, re-hash the ComfyUI models (ART-PIPELINE section 9), re-run the approved-art check.
- Never run `npm ci` in this tree while dev servers are up (it half-deleted `node_modules` on 2026-09-19;
  `npm ci --offline` restored it). Releases are cut from `D:\pyrefly-release`; delete its `dist-gate/` before deploying.

---
*Older snapshot (2026-09-19 08:05 EDT) kept below for the track tables; where it disagrees with the block above, the block above wins.*

> **Snapshot: 2026-09-19 08:05 EDT**, written by a side session from `git`,
> `docs/deploys.log` and the driver session's 22:15 notes. Whoever drives the project
> refreshes this file at the end of every work block. If this date is more than a
> day old, trust `git log -15` and `tail docs/deploys.log` over anything below.

## Live

- https://baileypillon.github.io/pyrefly-reprise/ = `main 5a82e71`, bundle `CF_L2xLd`, deployed
  2026-09-19 04:52 EDT (recorded in `97b1e56`), verified live with real key presses (Esc, P, H, N, E),
  prerendered audio playing, 0 console errors, 0 404s. Refreshed by the driver session 08:05 EDT.
- **Critic round 02 is complete** on that build: [critic/rounds/round-02.md](../../critic/rounds/round-02.md).
  Headline **3.5** (Part A 5.6, Part B 3.5, Part C 5.3; gate 9.6; Part C coverage incomplete). 45 ranked
  issues (26 blockers) and 5 proposals that need Bailey's yes. `critic/pending/` is clear.
- **Bailey's standing instruction (2026-09-19): everything is PAUSED except the concept boards**
  (`docs/concepts/polish/`, workflow running in the driver session). Do not start fixes from the critic's
  list, the fix-round-3 tracks, targeting, the audio fix pass or art until Bailey says resume.

## Who is active

- The Claude Code driver session "FFX/FFX-2 2.5D game recreation" runs the fix-round-3
  workflows. Its agents leave uncommitted work in the tree. At this snapshot:
  ~370 files under `docs/screenshots`, 60 under `tools/gen`, 21 under `src/audio`, and
  a few each in `src/ui`, `tests/unit`, `public/audio`, `src/app`, `src/data`, `src/story`.
- **If Claude's weekly allowance has run out, those agents are dead and their edits
  are still here.** Do not clean up. Before touching a modified file, read the matching
  `docs/handoff/fix3-*.md` and continue that work.

## In flight: fix round 3 (Bailey's criticisms of the `822ae16` build)

States are the driver's 22:15 notes plus `git log`; confirm before relying on one.

| Track | Fixes | State | Handoff |
|---|---|---|---|
| advisor | Never names a move the acting character lacks; state-aware revive; plain-words card | `01d4f6c`, `3aa5440`; verifier refuted 3 points once, fix pass under way | [fix3-advisor](fix3-advisor.md) |
| ffx-hud | Stale "Ronso Rage" banner, card and chips over sprites, CTB names truncated | `ffc516b`; builder still running | [fix3-ffx-hud](fix3-ffx-hud.md) |
| ffx2-hud-prep | FFX-2 party-row portraits, measured face crops, prep tabs | fix pass 2 (`bf0e017`) | [fix3-ffx2-hud-prep](fix3-ffx2-hud-prep.md) |
| pause | Full-bleed 2x plates, viewport-relative type | in verification; `PauseScreen.ts` modified, uncommitted | [fix3-pause](fix3-pause.md) |
| audio | Sampled-orchestra prerender: 21 cues + 134 SFX (`e7a6ed5`); fix pass for QA's 7 findings (`6e8c145`) under way | **Bailey judges by ear**: `docs/audio/audition.html` | [AUDIO-GUIDE](../AUDIO-GUIDE.md), [THEMES](../audio/THEMES.md) |
| targeting | Bailey's criticism 8, built to the approved option B (`docs/concepts/targeting/b-ring-and-dim/`): bracket scaled to the figure, ink name plate with its letter tag, FFX's hand / FFX-2's flower, accent pool + quiet dim, spread formation measured against the camera | **done**, `83ea60a` `177c0ac` `8653b63` `764528e`; verified live in all five chapters (107/109 checks, GPU mode), two open questions for Bailey | [fix3-targeting](fix3-targeting.md) |
| art track 3 | X-2 cast and the X-2 bosses fix remain | Earlier art was painted with a corrupt checkpoint and IP-Adapter (replaced 21:43, checksums in `811682d`): check the A/B before spending GPU on re-rolls | `art3-*.md`, [ART-PIPELINE](../ART-PIPELINE.md) |
| concept boards | 22 polish ideas as mockups in `docs/concepts/polish/` | **mockups only**; nothing is built until Bailey picks | |

## Next tasks any agent can take (no GPU, each one testable)

From the pre-deploy gate at 22:15. **Reproduce each one first**; another agent may
have fixed it since.

1. Party prep ITEMS / EQUIPMENT print raw ids (`strength-10` reads as a penalty): show display names.
2. Sphere Grid shows `K1`-`K4` labels and a debug zoom readout to players: hide them.
3. The polaroid caption still ships with an ellipsis.
4. Chapter select's hint says LEFT/RIGHT for a vertical list and overflows at 390 px wide.
5. Phone viewport (390x844) is a letterboxed strip with unreadable text on every screen
   except pause. Needs a layout decision: mockup first, then Bailey.
6. Chapter 1 did not reach a first player turn within 40 s on a saturated machine
   (unproven). Re-test on a quiet machine before calling it a bug.

Then, in the owner's order: advisor correctness → pause screen → audio → layout
collisions → targeting.

## Closed recently (do not redo)

All four engine follow-ups from 09-16 are done and verified in this snapshot: the timed
Overdrive bare re-submit loop in both engines, the possessed-aeon data (Passado is
15 hits), the `effectiveStats` export (`src/battle/ffx/effectiveStats.ts`), and the
`PartyPrepScreen.ts` split (372 lines). Details: [play-engine-followups](play-engine-followups.md).

## Owner decisions in force

- **Critic policy v2 (Bailey, 2026-09-20): every deployed build is evaluated, and the depth of the review
  follows what changed.** Deployment verification, changed-area acceptance and milestone acceptance are
  separate verdicts; one weighted score (9.60 unrounded, every category at least 9.0) plus gates;
  approved-target comparison is a gate; unknown or stale evidence is never a pass; an old score never
  certifies a new build. `critic/RUBRIC.md`; `node tools/critic-plan.mjs` says which review a change needs.
  The "full round after every deploy" rule is replaced by this; rounds 02 and 03 stay as rubric v1 history.

- **Standing rule from 2026-09-19 (Bailey): every change is specific and game-aware.**
  True to FFX but not FFX-2: it does not apply to FFX-2. True to FFX-2 but not FFX: it
  does not apply to FFX. True to both: it applies to both. Decide from the sources
  (`research/*.md`, `research/ffx-vs-ffx2-presentation.md`), write the case in the plan,
  handoff and commit. AGENTS.md hard rule 14; the critic checks it as CHK-021.

- Release order: the gameplay-fix build first, the audio build second, then **pause all
  work** until Bailey says go.
- Local art generation is **on** (resumed the evening of 09-18) until that pause.
- Run two or three workflows at a time, and the release gate alone on a quiet machine.
  Six at once exhausted the 5-hour allowance at 21:45 and killed every agent mid-work.
- A release gate stops only for regressions; pre-existing defects get labelled and listed.

## Log (newest first; keep the last ten)

Template:

```
### YYYY-MM-DD HH:MM — <tool / session>
- Did:
- Verified by:
- Left uncommitted:
- Next:
```

### 2026-09-21 (evening) — Claude Code sub-agent (decisions index on the end-state board; nothing deployed)
- Did: game case **both** (records and tooling, no game behaviour). `tools/end-state-board.mjs` now renders `docs/target/decisions.json` under the tiles (new sibling module `tools/end-state-board-decisions.mjs`, 132 lines; board script 346 lines): proposed decisions first under "Awaiting Bailey's verdict", then adopted newest first, superseded last and muted; counts computed from the data; a missing or malformed `decisions.json` warns and the board still builds. `--decisions <path>` overrides the file. `decisions.json` gained D-022 to D-028: Bailey's project decisions that were on record only in an agent's private memory (deploy every green checkpoint and announce it; delegation and the top model's three jobs; never weaken a boss / measured options; release pacing; modern music; the Leblanc renders as the painting quality bar; a pick is never reopened). Every `words` value was checked verbatim against a source file; D-024 has none and is null.
- Verified by: the main session — board rebuilt (84 tiles, 28 decisions: 24 adopted, 1 superseded, 3 proposed), all 28 ids present in `docs/target/board.html`, screenshot `docs/screenshots/board-decisions.png`.
- Left uncommitted: `tools/end-state-board.mjs`, `tools/end-state-board-decisions.mjs`, `docs/target/decisions.json`, this entry. Nothing committed by this session.
- Next: D-011 still says "Braska's headdress repaint goes past Bailey first", which D-028 records as closed — the owner of D-011 should correct that clause. Still awaiting Bailey's verdict: D-010, D-012, D-013. The new module has never been committed; the published board artifact has not been republished.

### 2026-09-21 15:30–21:00 EDT — Claude Code side session "learning sites" (all three sites BUILT and running locally; committed, not pushed, nothing deployed)
- Did: Bailey approved the nine frames ("yes commit it, all three looks are good", then "Links to all 3 websites please?"); verdict written into `docs/concepts/atlas/README.md`, `options.json`, `docs/plans/learning-sites.md`, nine approved tiles in `targets.json` (group `learning-sites`). Built, one agent at a time in conserve mode (Sonnet for the shell and narrow fixes, Opus where the look had to match a frame): `learn/vite.config.ts` + `learn/tsconfig.json` (own Vite entry, dev port 5310, `publicDir ../public` so art and fonts are served and never copied, `dist-learn/` with `copyPublicDir: false`), the DOM shell `learn/shared/shell.ts` `mountExplorer` (systems panel, explode slider, cited detail card, search, view rail, switcher, CSS 3D stage; a site may bring its own stage painter), three themes, and the three pages: **A `/atlas/`** (all five chapters from the real data; Vegnagun ported from its frames: assembled machine with battle pins and the chain card, stacks of up to three cards beside their own part on threads, named flow inventory of 119), **B `/studio/`** (one FFX turn; dragging Tidus's Agility re-runs the real `FFXEngine` and the on-page turn list equals the engine's under node; FFX-2 switch shown disabled, no FFX-2 data invented), **C `/exploded/`** (the Yunalesca frame from the real seed-1 engine run, eight glass sheets plus three rails, 141-piece inventory with 119 WebP thumbnails in `learn/exploded/thumbs/`, 1.3 MB). Commits `0f8fb0f`, `94021a8`, `5216520` (after `21a753b`). **Nothing under `src/`, `public/` or the root configs changed**; game case: both as a surface, each specimen FFX only or FFX-2 only with its accent from the chapter's `game`.
- **View them:** preview entry `learning-sites` in `.claude/launch.json` (`npx vite --config learn/vite.config.ts --port 5320 --strictPort`) → `http://localhost:5320/atlas/`, `/studio/`, `/exploded/`. Local only. **Publishing needs Bailey's decision** (a folder of the existing Pages site, which then goes through the release process, or a site of its own); nothing was deployed.
- Verified by: the main session re-ran `npx tsc --noEmit -p learn/tsconfig.json` (clean) and `npx vitest run tests/unit/learn-` (19 files, 199 tests) after every stage, read every target-beside-build pair (`docs/screenshots/learn/pair-a1..c3.jpg`), and drove site A by hand in the browser pane (select, keyboard slider, 0 console errors; the pane's simulated mouse could not drag the native range, the builders' Playwright mouse could). Each builder's real-input Playwright pass: 0 console errors, 0 failed requests on all three. Full `npm test` NOT run (nothing under `src/` changed).
- Lessons: a workflow launched while Bailey's last message was a question built nothing, twice in effect (agents answered the question; see memory `subagents-see-last-user-message`: quote the message as already answered, quote the approvals, and record the verdict ON DISK first, because agents check). A Sonnet first pass got the chrome right and the stage wrong (no on-stage size in the model); `Piece.stage / parentId / badge` and `Specimen.idle` fixed it at the contract.
- Open (polish, none blocks viewing): B's backdrop has a hard edge where the frame fades it; B and C stages are fitted to the shell's chrome-free box, narrower than their frames; C's switcher chips are cramped and its card is headed by the component name; inventories pan by drag instead of fitting one screen; chapters I to IV of site A use a procedural arrangement (no approved frame); no phone layout (no target); the Vegnagun head/tail/leg art at `public/art` has changed since the frames were shot (art track's call). Waiting on Bailey: where to publish, how plainly site C says the art is AI-generated, phone layout yes/no, whether `learn/exploded/thumbs/` may stay on `main`.

### 2026-09-21 01:00–03:10 EDT — Claude Code side session "learning sites" (new track; targets shown, plumbing built, nothing visible built, nothing deployed)
- Did: Bailey asked for "an interactive learning website" adapted from Human Atlas and Model X Studio (section 16 of https://youtu.be/ifz8NGHuHtY; both sites used first-hand, write-up `docs/concepts/atlas/REFERENCE.md`: one specimen on a stage, a systems panel with counts, the explode slider from assembled to a flat inventory, a sourced detail card with two tabs, isolate, search; pattern only, no code or asset copied, hard rule 8). Options round `docs/concepts/atlas/` (README, `options.json`, `sheet.png`, nine 1600x900 frames, each set adversarially fact-checked and repaired: 7 wrong numbers corrected, all "ready with caveats", no blockers). Bailey, before the frames existed: **"A, B, C all separately please but please be mindful of delegation"** = three separate sites: A "Pyrefly Atlas" (a boss taken apart; Vegnagun, 119 sourced pieces), B "Pyrefly Studio" (one turn taken apart, the real FFX engine computing the numbers live), C "Pyrefly Reprise, exploded" (one battle frame taken apart into layers). **The looks are NOT approved: the frames are with Bailey awaiting a verdict.** Plan: `docs/plans/learning-sites.md`. Built (plumbing only, no DOM/CSS): new root folder `learn/` = `learn/shared` (model that refuses a piece with no cite, store, layout, inventory packer, search), `learn/atlas` (any of the five chapters → specimen from `src/data/**` + the guide text with per-sentence cites), `learn/studio` (`runExampleTurn` on the real `FFXEngine`, seed-1 numbers identical to the mockup's engine probe; 137 cited rules), `learn/exploded` (nine layers + asset inventory from the art manifest passed in at runtime; party/enemy role from the data, not from `facing`). 3,900-odd lines, 9 test files. **The game's `src/`, `public/`, `vite.config.ts`, `tsconfig.json`, `vitest.config.ts` are untouched; `learn/` only imports `src/battle/**` and `src/data/**`**, so `critic-plan` sees no shared-system change and the Build A.2 release is unaffected. Game case: **both** as a surface; every specimen is FFX only or FFX-2 only with that game's accent (derived from the chapter's `game`, never set by hand); a turn list is FFX only.
- **Bailey restated the delegation rule in capitals ("DO NOT FORGET THIS NO MATTER WHAT"):** mechanical implementation, file searches, tests and routine execution go to sub-agents on Opus / Sonnet / Haiku; Fable is reserved for planning, architecture review and final validation. It now opens `~/.claude/CLAUDE.md` verbatim. **Set `model` on every `Agent` call and every workflow `agent()`**; omitting it inherits Fable. This session's first workflow (the mockup round, 7 agents) omitted it and cost about 11 weekly points and 19 Fable-weekly points; everything after ran on Sonnet.
- Side findings from the fact-checkers, flagged as task chips, not fixed here: (1) Bulwark record blocks Str/Mag breaks against its own comment and research §3.3 (a separate session is on it); (2) the chapter 5 guide says "cast Reflect on the Leg" while §3.2 and the data make the Leg Reflect-immune, and several researched steals are missing from the data; (3) `docs/ARCHITECTURE.md` lags the code (about 22 battle events listed, 36 in the union; music described as synthesis). Also: painted subjects `jecht` and `lenne` are named by no party or enemy record (story-only), so site C lists them as unclassified.
- Verified by: the main session re-ran everything itself: `npx tsc --noEmit` exit 0; `npx vitest run tests/unit/learn-` = 9 files, 107 tests green; no file over 400 lines; the nine frames looked at on the sheet and A2 at full size. Full `npm test` NOT run (nothing under `src/` changed; run it before any push). Usage: 5-hour 19 → 59, weekly 21 → 32, Fable weekly 22 → 41 across the mockup round.
- Left uncommitted: `learn/`, `tests/unit/learn-*.test.ts` (9), `docs/concepts/atlas/`, `docs/plans/learning-sites.md`, this entry. Asked Bailey whether to commit to `main` without pushing.
- Next: **wait for Bailey's verdict on each look (A, B, C)**, how plainly site C says the art is AI-generated, where the sites are published, and whether a phone layout is in the first build. Then: the DOM shell to the approved looks (Opus), the three pages wired to it (Sonnet), one real-input browser check per site with target beside build, targets recorded in `docs/target/targets.json`. Nothing visible is built before the verdict (hard rule 9).

### 2026-09-21 (late evening) — Claude Code side session (six process rules adopted from `project-scaffolding`; nothing deployed)
- Did: read Bailey's private repo `BaileyPillon/project-scaffolding` (read-only, GitHub API), wrote six proposals (`docs/plans/scaffold-proposals.md`), and on Bailey's **"adopt all six"** put them in force. (1) Stagnation: RUBRIC §8, `cadence.stalledAfterReviews`, `stalledIssues` in `tools/critic-policy.mjs`, `npm run critic:status` prints `STALLED:` (information only). (2) Repair counting and a cap per release candidate (2 / 1 / 0 cycles by usage mode; "take the failing change out and ship what passed"), `attempts` validated in reports. (3) Usage modes normal / conserve / protect in RUBRIC §9 and `policy.json`, the mode line at the top of this file; "protect at 80 percent used" replaces "ask above 85". (4) `delivery` / `verifiedBy` on target tiles (24 filled: 15 verified and 5 failing copied from round 04 by exact label, the three onboarding tiles, the Turn cut-in) and `docs/target/decisions.json` (14 decisions with their state). (5) `reaction` on tiles: what Bailey named versus what an agent inferred (targeting B, onboarding C, Ink & Gold, Braska's portrait). (6) `critic/calibration/cases.json` (four confirmed should-fail cases from CHECKS.md, four candidates to confirm) and a paper-preflight row in RUBRIC §4. AGENTS.md rule 15 summarises it. Game case: **both** (process only; every path is `no-product-effect`, so no review is owed).
- Verified by: `npx tsc --noEmit` clean; `tests/unit/critic-policy-adoptions.test.ts` (12 new tests) plus the five other test files that read `policy.json` / `targets.json`: 165 green; `node tools/critic-status.mjs` and `node tools/end-state-board.mjs` still run (73 tiles). The full `npm test` was not run.
- Committed as `62f4cbb` (not pushed): only this session's paths, staged one by one. Left uncommitted: this file, because it also carries another session's edits. Releases are cut from the clean worktree, so push `main` before the next cut for these rules to apply there.
- Not done, on purpose: no calibration run (size it and show Bailey first); delivery status is not shown on the board (needs a mockup and Bailey's pick, hard rule 9); 49 tiles have no `delivery` yet because no report names them by exact label.
- Next: run the paper preflight on the FFX-2 Active-only ATB build before it starts (RUBRIC §4); fill `attempts` and per-tile results in the round 05 report so `delivery` and `STALLED:` have data.

### 2026-09-20 (afternoon) — Claude Code sub-agent (onboarding option C: adversarial fix pass; nothing deployed)
- Did: an adversarial verifier refuted four properties of the onboarding build `b8a905f` on the running game, and this pass fixed each at its root. (1)+(2) Enter and Escape skipped Auron's briefing **and** were acted on by the screen behind it one frame later — first launch went title → briefing → party prep with the chapter board never seen, Escape backed the board out to the title, and the pause replay re-raised itself on every confirm, forever. The cause was in `src/app/Input.ts`: `claimKeyboard` stopped the DOM event in the capture phase but `onKeyDown` still latched the abstract button. `claimKeyboard` now takes `{ exclusive: true }`: a swallowed key latches no button, screens behind read no button/axis/action while the claim is up, and the frame after it is handed back drops every pending edge (which also settles the gamepad, where the two poll loops' order used to decide the outcome). `PauseScreen` forwards the briefing nothing; `Briefing.handleInput` and the `driven` option are gone; the briefing's pad watcher ignores the HUD mute (`RawInputWatcher { ignoreSuspend }`) so the pause replay is still dismissible by pad. (3) The FFX-2 line's badge claimed "Nothing paused · gauges running" while the gauges were measurably frozen (8189 ticks → 8189 across 1.5 s in both X-2 chapters, **identical with `?coach=off`** — `BattlePresenter` awaits `HudPort.chooseCommand` and ticks the FFX-2 engine only in its `waiting` branch). The badge now reads "Keep playing · nothing to press", which is what the line itself does. (4) The test meant to prove the clock kept running ticked the engine itself and then asserted it had moved; replaced by two mutation-checked assertions that can fail. Commits `e30ea5e` (code + tests) and `6cd337e` (handoff, the C3 target tile's knowing departure from the mockup's badge label, re-captured shots and pairs). Game case: **both** — `Input.ts` is shared plumbing and these are bug fixes (CHK-020); the badge wording is FFX-2 only.
- Verified by: `npx tsc --noEmit` clean; full suite **158 files / 4 229 tests green** (4 221 before); `node tools/orphans.mjs` clean for `src/ui/coach/`. New `tests/unit/ui-coach-input-leak.test.ts` (7) drives a real `Input`, a real `Briefing`, real `KeyboardEvent`s and a stubbed `getGamepads`. One browser pass on my own Vite server (port 5731, `--strictPort`, stopped afterwards), `PYREFLY_BROWSER=gpu` throughout, **re-running the verifier's own probes unchanged**: first-launch Enter now lands on `chapter-select` (was `party-prep`), Escape/click/wait-out all `chapter-select` (Escape was `title`), title replay stays on `title`, the pause replay's first Enter dismisses with `created 0` (was an endless loop) and Escape leaves `.pause__row` 12 → 12 with `screen: pause` (was the pause menu closed). Shots and the three target-versus-build pairs rebuilt under `docs/screenshots/onboarding/` and looked at.
- Left uncommitted: nothing of mine.
- Next: **a question for Bailey before anything else here** — should the FFX-2 ATB run while you are choosing a command? It does not today, teaching or no teaching; that is the Active/Wait setting (`research/ffx-vs-ffx2-presentation.md` §4.2, row "Command input"), which the build does not implement. FFX-2 only, combat-core, changes every X-2 fight's difficulty, so hard rule 10 applies. Then: this is a shared-system change (`src/app/Input.ts`), so `tools/critic-plan.mjs` will ask for a **deep review before going public**, and the first-time-player pass is still unrun.

### 2026-09-20 01:45 EDT — Claude Code side session (critic policy v2 integrated; nothing deployed, the pause stands)
- Did: Bailey approved the consolidated critic ("my approval of the proposed scoring, gates, cadence, and resource controls"). Integrated it: `critic/RUBRIC.md` is now policy v2 (three verdicts, a schedule where review depth follows the change, ONE weighted score over ten categories, acceptance gates, the approved-target gate that replaces Part C as a number); the old rubric is `critic/archive/RUBRIC-v1-ABC.md` and rounds 02 and 03 stay rubric v1 history. `critic/policy.json` holds the same rules as data. `critic/CHECKS.md` keeps CHK-001 to CHK-021 and B1 to B4 and adds CHK-022 (outcomes reach their destination), CHK-023 (subsystems are invoked through the real path), CHK-024 (saves survive an upgrade). New tools: `tools/critic-policy.mjs` (plan, score, gates, report validation), `critic-plan.mjs`, `critic-clear.mjs` (the only way an obligation is settled), `critic-score.mjs`, `artifact-manifest.mjs` (identity of every shipped file + byte-for-byte live verification); `critic-pending.mjs`, `critic-status.mjs` and `deploy-pages.mjs` reworked: a deploy now plans its review, refuses a shared-system change with no passing deep report, verifies the exact live artifact, and records separate `live` / `focused` / `deep` / `milestone` obligations; a replaced build's unsettled deep review moves to the new build. Review workflows: `critic/runner/release.js`, `focused.js`, `live.js`, `deep.js`; the driver's `pyrefly-release-clean.js` is now a copy of `release.js`, and the old `pyrefly-critic-round-2.js` and `pyrefly-critic-gate.js` refuse to run for a new build (backups beside them as `.pre-policy-v2.bak`). Seeded `critic/ledger.json` (baseline: round 03 on `7191674`, evidence only) and `critic/artifacts/7191674.json` (720 files, 367.7 MB, all media decode).
- Verified by: `tests/unit/critic-policy-v2.test.ts` (51) and `tests/unit/artifact-manifest.test.ts` (10) plus the 19 existing marker tests, `npx tsc --noEmit` clean; `node tools/critic-plan.mjs` on the real tree (docs-only since `7191674`: LIVE; a pause caption: FOCUSED; `src/battle/ffx/ctb.ts`: DEEP before deploy); `verify-live` against the real site compared 48 files byte-identical and still answered UNVERIFIED because build `7191674` published no manifest (fail closed, as designed); `deploy-pages.mjs --dry-run` prints the plan; all workflow scripts pass a syntax check. **Not executed: the four review workflows and a real deploy with the new script** (paused, allowance at 99 percent).
- Left uncommitted: everything above. **The release worktree is cut from committed `main`, so none of this applies to a release until it is committed;** `release.js` stops at the cut with that message if the tools are missing.
- Next: commit the critic integration; at the next release watch the first `live.js` and `focused.js` runs and tune the time budgets after three real runs (RUBRIC §4).

### 2026-09-19 (afternoon) — Claude Code side session (targeting pick on the board)
- Did: at the driver session's request, recorded Bailey's targeting pick ("B: hand, ring and a quiet dim") in `docs/target/targets.json` as three approved tiles (`docs/concepts/targeting/b-ring-and-dim/s1..s3.png`) with their game-aware cases and build hints; updated the Leblanc tile (Chateau Leblanc), the battle-transition tile (canon by situation) and the chapters note (they wait for the 25 Sep allowance reset). Board republished: 50 approved, 0 awaiting a verdict, 9 gaps, 2 rejected.
- Verified by: the three pictures viewed on a contact sheet before publishing.
- Left uncommitted: `docs/target/targets.json`, this entry.
- Next: remaining gaps are the move advisor card, the enemy next-move panel, the defeat screen, the cold open, the phone layout, the three chapters' concept sheets and one finished minute of play.

### 2026-09-19 (afternoon) — Claude Code side session (game-aware rule)
- Did: recorded Bailey's new standing rule (every change is FFX-only, FFX-2-only or both; see "Owner decisions in force") as `AGENTS.md` hard rule 14, `critic/CHECKS.md` CHK-021, a note in `docs/PRODUCT-BRIEF.md`, and memory `game-aware-changes`; told the busy driver session by message.
- Verified by: nothing to run; docs only. CHK-021 is written to sit beside CHK-020 (shared plumbing and bug fixes are "both").
- Left uncommitted: those four files and this entry.
- Next: every plan, handoff note and commit states its case and its source.

### 2026-09-19 (afternoon) — Claude Code side session (Bailey approved the end-state board)
- Did: Bailey said "i approve everything you listed in the end state board and please let the other agents know". `docs/target/targets.json` now has 47 approved tiles, 0 awaiting a verdict, 10 gaps, 2 rejected. Newly approved: the four other scene backdrops, ten cast tiles (FFX party portraits, Tidus, Yuna, Auron, Seymour Flux, Yunalesca 1, Braska's Final Aeon 1, Shiva, Yuna Gunner, Shuyin), the rebuilt pause screen and every pause plate, the concept key art, and audio as a direction ("Right direction, keep refining" still stands). Also filed the 12 concept boards Bailey picked as approved tiles (with their FFX-only / FFX-2-only notes), and the three new chapters and the cold open as gaps. Board republished (same URL, version 3).
- **What it means for every agent:** an approved painting is never replaced on an agent judge's say-so; a change needs Bailey's yes (the approved face pass on the weak close-ups is such a yes for the close-ups it names). sha256 of the 94 shipped files behind the approved tiles: `docs/target/approved-hashes.json`; backup copy: `D:\Tools\pyrefly-art-backup\approved\2026-09-19-board` (public/art has no git history). Part C of the rubric scores every approved tile from its `build` hint. **Targeting is still a gap: Bailey has not picked a look**, so a cheap options round comes before the staged targeting track builds.
- Also new: `docs/PRODUCT-BRIEF.md`, a draft from Bailey's picks (faithful recreation + 2026 showpiece; "faithful core, showpiece surface" settles conflicts; seasons; "one more try" from the fight itself), waiting for Bailey's yes.
- Verified by: none of the board's pictures or the shipped files behind them changed after the board Bailey looked at was generated (mtime check); the 12 concept mockups viewed on a contact sheet before publishing.
- Left uncommitted: `docs/target/targets.json`, `docs/target/approved-hashes.json`, `docs/PRODUCT-BRIEF.md`, `AGENTS.md`, `critic/RUBRIC.md`, `tools/end-state-board.mjs`, this entry.
- Next: when Bailey picks a new target, record it in `targets.json` (state, their words, date, `build` hint), run `node tools/end-state-board.mjs`, republish to the same artifact URL.

### 2026-09-18 23:40 — Claude Code side session (rubric Part C)
- Did: on Bailey's word ("you can add that to the rubric as part C") added **Part C: fidelity to the approved end state** to `critic/RUBRIC.md` (Parts A and B untouched). It scores only `approved` tiles in `docs/target/targets.json`, from target-vs-build composites; the gate is now 9.6 on all three parts plus complete coverage of the board. The staged round script `pyrefly-critic-round-2.js` gained one Part C auditor (sonnet, medium effort), three totals in the chief's report and a "Waiting on the owner" list; backup beside it as `.pre-partc.bak`. `tools/end-state-board.mjs --pair <target> <build> --out <file.jpg>` makes the side-by-side; approved tiles carry a `build` hint and the Zanarkand tile its `sha256`.
- Verified by: the script's body passes `node --check` (wrapped in an async function) and its `meta` still evaluates as a literal; `--pair` run on the title mockup vs the live title frame; `targets.json` parses. **The edited round script has not been run yet.**
- Left uncommitted: `critic/RUBRIC.md`, `AGENTS.md`, `docs/target/targets.json`, `tools/end-state-board.mjs`, this entry.
- Next: the next full critic round is the first with Part C; check its `part-c/` composites and the chief's three totals.

### 2026-09-18 23:15 — Claude Code side session (end-state board)
- Did: Bailey approved "end state first" as a standing rule (AGENTS.md hard rule 9). Added `docs/target/targets.json` (approved targets, pictures awaiting Bailey's verdict, gaps) and `tools/end-state-board.mjs`, which renders it to `docs/target/board.html` (gitignored). Published copy for Bailey: https://claude.ai/artifact/2qt2ZqrrdjRDwa5crZ4ifg
- Verified by: every picture on the board viewed on a contact sheet; one desktop and one phone screenshot of the page (no sideways scroll at 400 px); the game's Zanarkand backdrop has the same sha256 as the approved copy.
- Left uncommitted: `AGENTS.md` (rule 9 + map row), `.gitignore` (one line), `docs/target/targets.json`, `tools/end-state-board.mjs`, this entry. No product code touched.
- Next: **targeting needs an options round before its track launches** (it exists only in words). When Bailey gives a verdict, record it in `targets.json` with their words and the date, then rerun the script.

### 2026-09-18 22:55 — Claude Code side session
- Did: wrote `AGENTS.md`, `CLAUDE.md` (imports it) and this file so any agent can start cold.
- Verified by: every path and command in `AGENTS.md` checked against the repo; the four closed follow-ups checked in the source.
- Left uncommitted: `AGENTS.md`, `CLAUDE.md`, `docs/handoff/NOW.md` (docs only; nothing else touched).
- Next: the driver session owns this file from here.
