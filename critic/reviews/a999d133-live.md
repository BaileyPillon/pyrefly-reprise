```text
Build / artifact / target version: main a999d133, bundle Ji5E4fD0, artifactHash 612bae4ccada27c29c9ff2eadf43705242b2273d18d59c55ebf93f5cc3672e55
Review: live
Deployment: PASS
Changed area: NOT APPLICABLE
Ship: N/A (this is the post-deploy live check for a build already shipped under Bailey's owner override; ship verdict was recorded in the deep/focused history for this candidate, not here)
Milestone: not assessed
Quality: not assessed here (see last full score in critic/rounds/)
Targets: not assessed here (no target comparison run this pass)
Top issues: none critical or major found in this pass; one minor: `art/pause/tidus.png` request shows net::ERR_ABORTED in the network log (the 2x.webp variant loaded instead, 200; no console error, no broken pause portrait observed) — worth a follow-up look, not a regression, not blocking
Coverage: tested (below), reused: none, not tested: full 7-chapter first-menu sweep, FFX-2 Black Magic/Power Break exercise, phone prep-card scroll check, full CHK-024 upgrade matrix — all explicitly out of scope for this lightweight pass (see coverage.notTested)
Next required review and why: the deep review already owed by this build's predecessor chain (see docs/handoff/NOW.md / critic/pending) is unaffected by this live check; a fuller live sweep across all chapters would be warranted before the next milestone claim
Elapsed review time / repeated work avoided: ~18 minutes; manifest verification code and the debug API (`window.__pyrefly`) were reused rather than rebuilt
```

# Live verification — build a999d133 (bundle Ji5E4fD0)

## 1. Exact artifact (CHK-017)

`node tools/artifact-manifest.mjs verify-live --manifest critic/artifacts/a999d133.json --url https://baileypillon.github.io/pyrefly-reprise/ --changed-from critic/artifacts/e3b8c2a3.json`

```json
{
  "result": "PASS",
  "artifactHash": "612bae4ccada27c29c9ff2eadf43705242b2273d18d59c55ebf93f5cc3672e55",
  "liveManifest": "match",
  "checked": 95,
  "mismatched": [],
  "missing": [],
  "wrongType": [],
  "errors": [],
  "notes": []
}
```

95 shipped files compared byte-identical with the correct content type against the previous live build's manifest baseline (`e3b8c2a3.json`). The manifest for `a999d133` exists at `critic/artifacts/a999d133.json`, so this is a genuine PASS, not a bundle-name coincidence.

## 2. Real-input smoke (built-in Browser pane, 1600x900, fresh tab, cache-busting query `?livecheck=a999d133-1727222222`)

Real keyboard input throughout (`Return`, `Escape`, `h`) plus one real mouse click to bring the WebGL canvas into focus after a backgrounded-tab render stall (see note below). Every screen/state claim below was confirmed with `window.__pyrefly.screen()` / `.battleState()` / `.audioDebug()` / `.app.save.data`, never assumed from a screenshot alone (CHK-016).

- Title screen loaded correctly (painted key art, "PRESS ENTER" prompt, `arrows/wasd move · enter confirm · esc cancel · b briefing`).
- Real `Enter` → Auron's briefing screen (skippable text). Real `Escape` → confirmed `screen() === "chapter-select"`.
- Chapter select screenshot confirms the intended card set: FFX I Seymour Flux, II Lady Yunalesca, III Braska's Final Aeon, VIII Evrae, and a locked "Seymour and Anima" **COMING** card (Chapter VII); FFX-2 IV Bahamut, V Vegnagun, VI Leblanc. **No Chapter IX/X/XI card is present.**
- Real `Enter` → Party Prep for Chapter I (Seymour Flux), confirmed by on-screen chapter header and roster.
- Real `Enter` → pre-battle cutscene (real screenshots showed the establishing shot and a Kimahri line fading in); used the debug `skipCutscene()` helper to pass the unrelated narrative faster (not the mechanic under test) and confirmed `screen() === "battle"`.
- **Note on rendering:** immediately after skipping the cutscene, one screenshot showed the battle scene with a `PAUSE` debug badge and no HUD — this was the render loop stalled by a backgrounded browser tab (the pane had gone frontend-hidden during a prior call), not a game defect. Fronting the tab and clicking once inside the canvas restored full rendering immediately: command menu, party HUD, enemy HP/affinity panel, turn-order strip and the move-advisor/enemy-intent coach cards all appeared correctly. **Chapter I's first command menu is fully readable, with no party or enemy figure standing under the menu panel** (menu occupies the left third; the party stands center-right, clear of it).
- Pause: real `Escape` from the battle opened the pause screen on the Tidus stats tab — no clipping between the face art and the stats block. Real `h` hid the panels to the Until-Dawn-style full portrait close-up; real `h` again restored them; real `Escape` resumed play.
- Audio: `window.__pyrefly.audioDebug()` showed `playing: "boss-seymour"` with that track's own entry `{ cached: true, source: "prerendered" }` — confirms prerendered music is fetched and playing, not the procedural synth fallback. `prerendered.manifest === true`, `sprite === true`, `spriteDecoded === true`.
- Console: only `info`/`warn` entries the whole run (a chapter-select debug info line, a known "opaque white background cleaned at load time" art warning already tracked, and a THREE.js shadow-map deprecation warning). **Zero console errors.**
- Network: every request in the captured log returned `200` except one `art/pause/tidus.png` that shows `net::ERR_ABORTED` — its `.2x.webp` sibling loaded fine at `200` immediately after, and the pause portrait rendered correctly on screen, so this reads as a cancelled duplicate/format-negotiation fetch rather than a broken asset. **Zero `>=400` responses, zero `text/html` masquerading as an image.**

## 3. Reload smoke (CHK-024, lightweight)

Set `Settings.masterVolume` to `0.42` via the real save object (`window.__pyrefly.app.save.data.settings`, the same object the Options screen writes) while one attempt was already recorded on `seymour-flux` (`attempts: 1`, `playTimeMs` ~54.5s, from actually starting the battle above). Reloaded the page fully (`navigate` to the same URL). After reload: `masterVolume === 0.42` and `chapters.seymour-flux.attempts === 1` (`playTimeMs` had advanced slightly further, showing the save is live-updated, not stale). **Both the setting and the progress survived the reload.** The full upgrade matrix (fresh player / returning player / previous-release save migrating forward / invalid or truncated storage / reset-and-confirm) is **not tested** in this pass, as scoped.

## Coverage

**Tested:** artifact-manifest verify-live (95 files, byte-identical vs. e3b8c2a3 baseline); title → real Enter → briefing → real Escape → chapter-select (card set and locked/coming states confirmed); real Enter → party prep → real Enter → cutscene → battle for Chapter I (Seymour Flux); Chapter I's first command menu reachable and unobstructed; pause open/resume (Escape) and hide/restore (H) with no face/stat clipping; prerendered music confirmed playing (not synth) via the audio debug surface; console errors and network status codes for the whole run; a real save-backed setting plus real chapter-attempt progress surviving a full reload.

**Reused:** none (fresh browser context, no evidence carried forward for this pass).

**Not tested (explicitly out of scope for this 2-5-minute deployment check):** the full 7-chapter first-menu sweep (only Chapter I was driven end to end to a battle state; chapters II, III, VIII, IV, V, VI were confirmed present and correctly locked/unlocked on the chapter-select card only, not opened); the FFX-2 Chapter IV Black Magic / Paine Power Break exercise; the 390x844 phone prep-card scroll check; the full CHK-024 upgrade matrix (fresh/returning/migrating/invalid-storage/reset flows); a second independent "cleared chapter survives reload" case beyond the one attempts-counter check above; GPU renderer string was not printed (this pass used the built-in Browser pane's own Chromium context, not a standalone `PYREFLY_BROWSER=gpu` Playwright run — see note below).

**Renderer / browser note:** this pass ran in the desktop app's built-in Browser pane (Claude_Browser tools), not a scripted Playwright run, because the live URL only needed manual real-input verification for this small check; `PYREFLY_BROWSER=gpu` is a Playwright-run environment variable and does not apply to the built-in pane. The canvas rendered the full painted scene (not a black frame), so there is no GPU-state concern to report, but no renderer string was captured. A future pass that needs the literal `PYREFLY_BROWSER=gpu` evidence line should use `critic/runner/live.js`'s underlying Playwright harness instead.

Console errors: 0
Not-found (>=400) responses: 0
Elapsed minutes: ~18
