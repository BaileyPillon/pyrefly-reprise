Build / artifact / target version: main 1a680e41, bundle BVdUtZb-, artifactHash 1b5eba77b3ed49108942a02ab8fd4c0b923cfa0407835ef9708a375882702e3d
Review: live
Deployment: PASS
Changed area: NOT APPLICABLE
Ship: NOT APPLICABLE (this is a live/deployment check, not a ship decision; the focused review already recorded SHIP for this candidate, `critic/reviews/1a680e41-focused.json`, release 17.1 deployed 2026-09-25 21:30Z under Bailey's owner override "Push the build live please let's get moving")
Milestone: not assessed
Quality: last full score 9.60 unrounded (round 03, rubric v1, build 7191674, 2026-09-19) — never compared with a v2 score
Targets: not assessed in this pass
Top issues: none found in the tested scope
Coverage: tested = exact-artifact manifest verify-live; real-input smoke (title -> chapter select -> Chapter IV FFX-2 Bahamut to a win via a debug HP-lower setup + a real, in-game Attack -> results wedge shows FFX-2 X-2 art; pause/H/resume and prerendered-music check mid-battle; Chapter I Seymour Flux to a win the same way -> results wedge still FFX art; 390x844 real-touch phone smoke on Chapter IV: tapping Item then Lunar Curtain names the target card and Confirm button with the tapped item, targeting All allies) with state asserted before each capture; reload smoke (setting + Chapter I progress). reused = none. notTested = full CHK-024 upgrade matrix (fresh/returning/migrating/invalid-storage save profiles), Chapters II/III/V real-input smoke, a real cancel path on the phone target step (only the desktop cancel-equivalent — Escape out of the pause overlay — and the phone Back button's presence were exercised), physical device/Safari/controller input, the item's real submission and its damage/status resolution (the target card and Confirm label were read before confirming; the confirm tap itself was not required by this check and was not exercised on phone).
Next required review and why: the deep review carried since fd0ae96 (now 16 builds per `critic/pending/1a680e41.json`) is still pending; this live pass settles only the `live` obligation.
Elapsed review time / repeated work avoided: about 45 minutes (most of it building and adjusting the real-input path for Yuna's White Mage dressphere, which has no Attack — the FFX-2 "Change" command was used to reach Gunner's Attack, itself a real in-game path); no prior live report existed for 1a680e41 to reuse.

## 1. Exact artifact (CHK-017)

Ran:
```
node tools/artifact-manifest.mjs verify-live --manifest critic/artifacts/1a680e41.json --url https://baileypillon.github.io/pyrefly-reprise/ --changed-from critic/artifacts/e45ed3c1.json
```
Result:
```json
{
  "result": "PASS",
  "artifactHash": "1b5eba77b3ed49108942a02ab8fd4c0b923cfa0407835ef9708a375882702e3d",
  "liveManifest": "match",
  "checked": 47,
  "mismatched": [],
  "missing": [],
  "wrongType": [],
  "errors": [],
  "notes": []
}
```
The live URL serves this exact artifact: 47 files compared byte-for-byte against the changed-from diff (e45ed3c1 -> 1a680e41), all matching, correct content types, no errors. A matching bundle name was not relied on; every compared file's bytes were checked.

## 2. Real-input smoke (CHK-016)

Headless Playwright (`playwright` 1.63.0), `PYREFLY_BROWSER=gpu` (real GPU Chromium via ANGLE, no SwiftShader fallback needed — no black-canvas issue was observed, so no fallback was used), fresh browser contexts per part, cache-busting `?v=<timestamp>` query. Screen, battle-menu and outcome state asserted via `window.__pyrefly.screen()` / `battleState()` / the presenter snapshot's `awaitingMenu` before proceeding at every step (never a bare timeout). Script: `tools/zz-live-1a680e41.tmp.mjs` (agent scratch, left in place per the shared-working-tree rule; a companion exploratory script `tools/zz-live-1a680e41-phone.tmp.mjs` is also left, safe to delete).

**Part A — Chapter IV (FFX-2 Bahamut), 1600x900, win, wedge = X-2 art:**
- Booted to `title`, real click + real Enter reached `chapter-select` (asserted).
- Selected Chapter IV via the debug `select:` trigger (the same path `ChapterSelectScreen.trigger` uses for a real card click/Enter, per DEV.md) -> real Enter through `party-prep` -> `battle` (asserted at each screen).
- Reached a real player command menu (`awaitingMenu: true`).
- **DEBUG SETUP (labelled, the only debug mutation in this pass, per the task's own allowance)**: every enemy's `hp` was set to 1 directly on the live `BattleEngine.state()` object (`bahamut: 8400 -> 1`). This sets up the win; it does not perform it.
- **Pause / resume / hide / audio, before winning** (real keys): Escape opened the pause overlay (`app.screens` included `pause`); a real `H` hid `.pause__ui`, a second `H` restored it; `audioDebug()` showed `playing: "pause"` with `source: "prerendered"`, `cached: true` (HYMN "Still Water", bars 1-8) — prerendered music playing, not the procedural synth fallback; a real `P` did not close the pause screen on this build (noted below, not new — the same gap was recorded for `fc7f1a20`), a real Escape did, resuming to `battle`.
- **The real win**: Yuna's default menu here is White Mage (`White Magic / Change / Item`) — White Mage genuinely has no Attack command and no damaging item is in her Item list (Songstress/White Mage/Black Mage omit Attack, `src/battle/ffx2/dresspheres.ts`). The real, in-game path used was the top-level **Change** command (a real menu option, not a debug shortcut) to Gunner, whose menu then offered **Attack**; a real Enter opened the list (Attack first), a real Enter confirmed it, and the engine resolved `action-end -> atb -> victory`, enemy HP `1 -> 0`. Screen reached `results`.
- **Results wedge (FOC17-01/PR-0021, the changed area for this release)**: `imgSrc` = `/pyrefly-reprise/art/portraits/yuna-x2.png` — the FFX-2 (X-2) likeness, as the release note requires for an FFX-2 win.

**Part B — Chapter I (Seymour Flux, FFX), 1600x900, win, wedge = FFX art:**
- Real Enter through `party-prep` -> `battle` (asserted). Reached a real menu; the same labelled debug HP-lower (`seymour-flux: 70000 -> 1`, `mortiorchis: 4000 -> 1`) set up the win. Tidus's menu opens with Attack first (FFX has no dressphere gate), so the same real Enter x3 sequence (open list, confirm Attack, confirm default target) ended the fight -> `results`.
- **Results wedge**: `imgSrc` = `/pyrefly-reprise/art/portraits/tidus.png` — unchanged FFX art, no `-x2` suffix, confirming the fix is FFX-2-scoped and did not touch FFX (AGENTS.md rule 14 case: FFX-2 only).

**Part C — 390x844, real touch, phone target card + Confirm (FOC17-02, the release's other changed item):**
- Fresh mobile context (`hasTouch: true`, `isMobile: true`), reached Chapter IV's battle the same way; `data-phone-battle="ffx2"` confirmed the phone layout is active.
- A **real touch tap** (`locator.tap()`, which dispatches actual touch events) on Yuna's "Item" row (`.ffx2hud__command [data-idx="2"]`) opened the Item submenu (title read back as `"Item"`).
- A real touch tap on the "Lunar Curtain" row (`targeting: 'all-allies'`, `src/data/ffx2/items/status.ts`) moved the FFX-2 cursor to that row (the FOC17-02 fix) and opened the target step. Read from the live DOM:
  - `.phud-card__name` = `"All allies"`
  - `.phud-card__note` = `"Lunar Curtain · Grants Shell to the whole party."`
  - `.phud-target__go` (Confirm button) = `"Lunar Curtain → All allies"`
  - `.phud-target__hint` = `"All three girls at once · Confirm or Back"`
  This is the exact behaviour the release note asks for: the target card and Confirm button name the command actually sent (the tapped item), not a stale or generic label. The item's actual submission (a real tap on Confirm) was not required by the check and was not exercised in this pass — see notTested.
- **Console errors: 0. Network 4xx/5xx (404s etc.): 0** across the whole run (all three contexts).

## 3. Reload smoke (CHK-024, lightweight)

Changed `musicVolume` from 0.7 to 0.37 via `SaveStore.setSettings` (the real save-write path), confirmed it took effect, reloaded the page, and confirmed it read back as 0.37 after reload. Chapter I's just-recorded win (`seymour-flux`: `cleared: true`, `attempts: 1`, `bestTurns: 1`, `bestTimeMs: 17833`, `playTimeMs: 17149.2`) was compared before and after the same reload and was byte-identical. This is the lightweight CHK-024 pass only; the full upgrade-path matrix (older save shapes, fresh/returning/migrating/invalid-storage profiles) is **not tested** here, per the task's own scope.

## Notes

- `P` alone did not close the pause screen on Chapter IV where Escape did — the same gap already on record for `fc7f1a20`'s live review; not a regression introduced by this change, not investigated further here (scope is deployment identity + smoke, not a defect audit).
- This review only settles the **live** obligation for build 1a680e41. `critic/pending/1a680e41.json` lists `focused` as already done (SHIP, `critic/reviews/1a680e41-focused.json`) and `deep` as still pending, carried since `fd0ae96` through 15 prior builds; this report does not and cannot settle the deep obligation.
- No product code was changed, committed, pushed or deployed by this review.
