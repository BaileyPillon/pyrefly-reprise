Build / artifact / target version: main b975397b, bundle Dd1Fjf8_, artifactHash 1d4de5edb67b7be498709c77e824168d3e30493de36acb2469b8a0634f42c791
Review: live
Deployment: PASS
Changed area: NOT APPLICABLE
Ship: NOT APPLICABLE (this is a live/deployment check, not a ship decision; the focused review already recorded SHIP for this candidate under Bailey's owner override "come on please, godspeed. I have full usage.", `critic/reviews/b975397b-focused.json`, release 18 deployed 2026-09-26 06:27Z)
Milestone: not assessed
Quality: last full score 9.60 unrounded (round 03, rubric v1, build 7191674, 2026-09-19) — never compared with a v2 score
Targets: not assessed in this pass
Top issues: none found in the tested scope
Coverage: tested = exact-artifact manifest verify-live against the previous build 1a680e41; real-input smoke (title -> chapter select via a real click -> chapter-select v2 confirmed to list all four newly listed chapters, X Natus / XI Fallen Aeons / XII Omnis / XIV Isaaru, alongside the nine already-live cards -> entered X Natus via the two-click select+confirm rule -> real held-Enter cutscene skip -> real player turn reached with awaitingMenu asserted before proceeding -> positive path (real Enter opens the command menu) and cancel path (real Escape backs out / opens the documented pause state, battle never exited) -> pause/resume via Escape, H hides and restores the pause panels -> prerendered music confirmed active, not the synth fallback) with state asserted before each capture; reload smoke (one settings field and X Natus's incremented attempt count both survive a reload); console errors and HTTP >=400 responses counted for the whole run (0 and 0). reused = none. notTested = the full CHK-024 upgrade matrix (fresh/returning/migrating-fixture/invalid-storage save profiles), the other three new chapters played end to end (only their chapter-select listing was confirmed, not a full playthrough), a fresh regression replay of the nine already-live chapters (this pass relied on the exact-artifact check for their identity, not a new playthrough), physical device/Safari/controller input, and the 390x844 phone layout.
Next required review and why: the deep review carried since fd0ae96 (now 16 builds per `critic/pending/b975397b.json`) is still pending; this live pass settles only the `live` obligation.
Elapsed review time / repeated work avoided: about 22 minutes; no prior live report existed for b975397b to reuse.

## 1. Exact artifact (CHK-017)

Ran:
```
node tools/artifact-manifest.mjs verify-live --manifest critic/artifacts/b975397b.json --url https://baileypillon.github.io/pyrefly-reprise/ --changed-from critic/artifacts/1a680e41.json
```
Result:
```json
{
 "result": "PASS",
 "artifactHash": "1d4de5edb67b7be498709c77e824168d3e30493de36acb2469b8a0634f42c791",
 "liveManifest": "match",
 "checked": 50,
 "mismatched": [],
 "missing": [],
 "wrongType": [],
 "errors": [],
 "notes": []
}
```
The manifest for b975397b existed at `critic/artifacts/b975397b.json` (written by the deploy). The live site names this exact artifact and every compared file is byte-identical with the right content type. **PASS**, not a guess from a matching bundle name.

## 2. Real-input smoke (CHK-016)

Headless Playwright (chromium via the `playwright` package already in `node_modules`), `PYREFLY_BROWSER=gpu` (real-GPU launch args from `tools/browser-mode.mjs`; no black-canvas fallback was needed, so no swiftshader fallback was used), a fresh incognito context, viewport 1600x900, deviceScaleFactor 1. Script: `.zz-live-smoke-b975397b-tmp.mjs` (repo-root scratch file, AGENTS.md `.*-tmp.mjs` convention).

Steps, all via real key presses and mouse clicks (never `window.__pyrefly` used to skip an interaction, only to read state for assertions):

1. Navigated to `https://baileypillon.github.io/pyrefly-reprise/?cb=<timestamp>` (cache-busting query). Waited for `window.__pyreflyReady === true`. `screen()` = `'title'`.
2. Real click on the title's `[data-action="confirm"]` plate. `screen()` became `'chapter-select'`.
3. Read every `[data-card]` id on the board: all nine already-live chapters plus all four newly listed ones (`seymour-natus` = X Natus, `ffx2-fallen-aeons` = XI Fallen Aeons, `seymour-omnis` = XII Omnis, `isaaru-via-purifico` = XIV Isaaru) were present — chapter-select v2 lists the claimed set.
4. Real two-click select-then-confirm on the `seymour-natus` card (the board's documented two-click rule). Reached `party-prep`.
5. Real Enter began the chapter; the pre-battle cutscene needed a real **held** Enter (per `docs/DEV.md`, a tap only advances a line) to skip, reaching `battle`.
6. Waited for `window.__pyrefly.snapshotState().screenState.playback.awaitingMenu === true` before doing anything else — the state was asserted, not assumed (CHK-016's own rule). It became true.
7. Positive path: real Enter opened the command menu on the player's turn.
8. Cancel path: real Escape backed the menu out. Observed outcome was the documented one — either still `'battle'` or the separate `'pause'` state (Escape from the top of the menu opens pause, per `ChapterSelectScreen`/battle input handling); battle was never exited to chapter-select or title either way.
9. Pause / resume: real Escape opened `'pause'` with a pause panel present (`document.querySelector('[class*="pause"]')` truthy). Real `KeyP` then `KeyH` hid the pause UI (0 matching elements); a second real `KeyH` restored it (77 elements, matching the pre-hide count). Real Escape resumed play.
10. Music: `audioDebug()` (read-only introspection, not an input shortcut) showed the boss cue `boss-seymour-macalania` active with `source: "prerendered"`, and `prerendered.manifest === true`, `prerendered.spriteDecoded === true` — the shipped prerendered sprite is what is playing, not the procedural synth fallback.
11. Console errors and HTTP responses with status >= 400 were counted for the **entire** run (boot through reload): **0 console errors, 0 not-found/error responses**.

## 3. Reload smoke (CHK-024, lightweight)

On the same run: the X Natus chapter's `attempts` counter in the save (`pyrefly-reprise:save:v1` in `localStorage`) had incremented to 1 from real play. `page.reload()` was performed (a real browser reload of the live URL, not a soft in-app reset). After reload: `attempts` for `seymour-natus` was still 1, and the `pausePanelsHidden` settings field read back the same value it held before the reload. Both a piece of real progress and a settings field survive a reload.

**Not tested** (explicitly out of scope for this lightweight pass): the full upgrade matrix — fresh player, returning player, a fixture of the previous live build's `localStorage` migrating forward, reload during every allowed state, and invalid/truncated storage.

## Findings

None. No critical, major or minor defect was observed in the tested scope. The other three newly listed chapters were confirmed present and selectable on the board but were not played to a battle in this pass; a full playthrough of them (and a fresh regression replay of the nine already-live chapters, beyond the exact-artifact check) is left to the deep review still owed on this build.
