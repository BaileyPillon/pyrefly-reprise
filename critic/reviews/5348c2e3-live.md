Build / artifact / target version: main 5348c2e3, bundle CcBx7HBL, artifact e8e31b1856ea405a1743b8b569a5ac12939d55cad261ce98aa134f7cf178e5de
Review: live
Deployment: FAIL
Changed area: NOT APPLICABLE
Ship: NOT APPLICABLE (live review does not carry a ship verdict; this build already shipped under Bailey's owner override, "Just push the build live please", per critic/pending/5348c2e3.json)
Milestone: not assessed
Quality: not assessed by this review (see the last focused/deep report for the current full score)
Targets: not assessed by this review
Top issues: CHK-024-LIVE-1 -- settings changed in the pause OPTIONS panel (tried: Master Volume) are applied in-session but are not durably written to the save, so they revert on reload; major, evidence below, next correction is to find and fix the settings-write path (it looks like something re-persists a stale settings snapshot shortly after the real change, since one trial's immediate post-change read did show the new value before it was lost).
Coverage: tested — CHK-017 exact-artifact verify-live (full mode, changed-from 5ddfde3, run twice, 0 mismatched/wrongType/errors both times); CHK-016 real-input smoke across Chapter VI (Leblanc) and Chapter III (Braska's Final Aeon); prerendered-music-vs-fallback; CHK-024 lightweight reload smoke (three trials); console error / 4xx-5xx count for the whole run.
Coverage: reused — none (fresh live check against this build's own artifact manifest and pending marker).
Coverage: not tested — the full CHK-024 upgrade matrix (every setting, a cleared chapter, unlock state, a previous-build save fixture); a real 1600x900/390x844 device capture (pane-capture limitation, see notes); the final Syndicate-wide defeat at the end of Chapter VI's Battle 3 of 3 (only Battle 1 was played to its own defeat); the J-hold / MORE overflow chip for a clipped enemy-move description (no move description was long enough to clip this run); whether the CHK-024 defect is new to this candidate or pre-existing (would need the previous artifact re-served side by side).
Next required review and why: the focused and deep review obligations carried in critic/pending/5348c2e3.json (from fd0ae96/8f48237/1b33971/5ddfde3) are still owed; in addition, the CHK-024-LIVE-1 settings-persistence defect found here should get its own investigation/fix and a follow-up reload-smoke recheck before the next deploy.
Elapsed review time / repeated work avoided: ~55 minutes (well past the 2-5 minute small-change budget: the assigned smoke list actually covered two full chapters plus a live-artifact defect that needed three separate real-input trials to characterize; the CHK-017 manifest diff and the console/network evidence were captured directly from the live run rather than re-derived by hand).

## Notes

**CHK-017 -- exact artifact.** `node tools/artifact-manifest.mjs verify-live --manifest critic/artifacts/5348c2e3.json --url https://baileypillon.github.io/pyrefly-reprise/ --changed-from critic/artifacts/5ddfde3.json --full`, run twice:
- First run: `{"result":"FAIL",...,"checked":884,"missing":["art/characters/paine-gunner/idle.png (503)"]}`.
- Second run: `{"result":"FAIL",...,"checked":883,"missing":["art/characters/rikku-dark-knight/cast.2.png (503)","art/characters/yu-pagoda/idle.json (503)"]}`.

Both flagged files were re-fetched by hand immediately after with a cache-busting query (`curl`, 2-3 attempts each) and returned 200 every time. This is GitHub Pages throttling under the ~885-request full-manifest sweep (a known behaviour of serving that many files back-to-back from one client), not a missing or corrupted artifact: `artifactHash` matched `critic/artifacts/5348c2e3.json` and the last line of `docs/deploys.log` in every run, `liveManifest` was `"match"` every time, and `mismatched`/`wrongType`/`errors` were empty in both full runs. The live browser session independently confirmed `assets/index-CcBx7HBL.js` and `assets/index-BqHP4QuQ.css` both 200 under `/pyrefly-reprise/`, matching the recorded bundle hash. This is recorded as a genuine PASS on the artifact's identity, not UNVERIFIED.

**CHK-016 -- real-input smoke.** Real key/click input drove:
- Title -> Enter -> chapter select. Confirmed **Chapter VII "Seymour and Anima"** and **Chapter VIII "Evrae"** both still show as locked **Coming** cards.
- Right x5 -> **Chapter VI, Leblanc** -> Enter -> Party Prep (header "VI . Leblanc . Chateau Leblanc, Guadosalam"; Rikku shown as Thief) -> Enter -> pre-battle cutscene, advanced with real Enter presses, finished with `window.__pyrefly.skipCutscene()` -> battle. `window.__pyrefly.screen()` asserted `"battle"` before each capture.
- The FFX-2 Wait coach line **"Bar's full, she's up! Take your time, nobody moves while you're picking."** (Rikku, marked first-time-only) appeared on the very first FFX-2 menu reached by this save profile, matching the sourced coach copy.
- **Rikku's Steal**: SKILL -> STEAL -> targeted Ormi, real Enter presses throughout. `battleLog()` recorded `{"type":"message","text":"Rikku stole X-Potion!"}` immediately after the action -- a successful, real, positive-path steal.
- **Enemy-intent panel**: visible throughout (Fem-Goon's next move, odds table, per-character damage estimates); its own `E / HIDE` toggle was exercised; Escape opens the pause screen (a different, also-real path to conceal it).
- **Pause screen**: tab strip YUNA / RIKKU / PAINE / CHAPTER / GUIDE / OPTIONS / CONTROLS / MUSIC; no advisor card or damage numbers anywhere in the pause DOM/text dump; `H` hid every panel down to a full-bleed close-up portrait (screenshot-confirmed) and `H` again restored them; the **CHAPTER** tab showed Chapter VI's own plate and objectives, not a stale chapter's. At **390x844**, the OPTIONS tab's content container (`pause__body`) measured `scrollHeight === clientHeight === 95` -- it fits without overflow, so there is nothing to scroll and nothing clipped.
- **Syndicate defeat**: Battle 1 of 3 was played to its own defeat (real Attack + Steal, then `window.__pyrefly.autoBattle('intended')` to finish the remaining exchanges once the mechanic itself was already proven with real input). `battleState()` showed all three enemies `hp:0, alive:false, removed:false` -- they are not removed from the encounter. After a brief scene-transition fade, the screen showed Ormi and Logos still standing, visibly dimmed, with no pyrefly-dissolve effect. The full Syndicate-wide final defeat (end of Battle 3 of 3) was not reached this pass -- see `notTested`.
- **Chapter III, Braska's Final Aeon**: reached via `gotoChapter` + `skipCutscene` (debug hooks used only to set up this second state after the reachability mechanic was already proven with real input on Chapter VI, per RUBRIC section 5's "debug hooks may set up a state" allowance), then driven entirely by real input: Auron's **SWITCH** command -> **Lulu**; Lulu's **SPECIAL** -> **DOUBLECAST** -> a spell list appeared (Fire/Fira/Firaga/Blizzard/Blizzara/Blizzaga/...) -> **Fire** selected -> **Yu Pagoda** targeted -> Enter. `battleLog()` showed two separate `action-start`/`damage` pairs for ability id `doublecast` wrapping `fire` against `yu-pagoda-left` (679 then 683 fire damage, one MP paid per cast) -- Doublecast asks for a spell, then an enemy target, and casts twice, exactly as specified.
- **Prerendered music vs. fallback**: network log shows real `.mp3` GETs (`audio/music/title.mp3`, `chapter-select.mp3`, `scene-bevelle-underground.mp3`, `boss-ffx2-aeon.mp3`, `pause.mp3`), all 200; `window.__pyrefly.audioDebug()` reported the active track (`boss-ffx2-aeon`) with `cached:true, source:"prerendered"`.
- **Whole-run totals**: 491 network requests logged across the session, all 200 (0 4xx/5xx). 0 console errors -- only an `[info]` chapter-select dev note and a benign `THREE.WebGLShadowMap: PCFSoftShadowMap has been removed` deprecation warning.

**CHK-024 (lightweight reload smoke) -- FAIL.** Three real-input trials:
1. Chapter VI battle, pause -> OPTIONS, Master Volume real keypress 80 -> 60 (then a further press to 50 while re-checking). `audioDebug().volumes.master` read `0.5` in-session, matching the on-screen "50". `localStorage['pyrefly-reprise:save:v1'].settings.masterVolume` stayed `0.8` immediately after the change, and still `0.8` after an 8-second wait plus Escape/Resume out of the pause menu. The setting was applied live but never written to the save.
2. A fresh tab (same origin/localStorage), Chapter I (FFX/CTB) battle, pause -> OPTIONS, Master Volume 80 -> 60: this time the *immediate* `localStorage` read did show `0.6`, matching the in-session value.
3. Immediately after (2), without an intervening reload, a full `navigate()` was done to reset to a clean state and re-enter Chapter VI: the same `localStorage` key now read back `0.8` -- the `0.6` write from trial (2) did not survive even a same-tab full navigation, let alone a real player closing and reopening the tab.

In no trial did a changed Master Volume value survive a reload; it always reverted to the last durably-saved `0.8`. This reproduces the *class* of the historical CHK-024 failure recorded in critic round 03 ("saved audio settings were never applied at boot"), though the mechanism here looks different (the value is not durably written at all, rather than being written but not applied at boot). Chapter progress itself was unaffected in every trial: the pre-existing `seymour-flux` chapter record in the save was untouched.

**Regression status: UNKNOWN, not confirmed new.** A prior live check on build `1b33971` (`critic/reviews/1b33971-live.json`) recorded a single successful trial of exactly this kind of test (Master Volume 80 -> 90, reload, value and a chapter-attempt count both survived). That is suggestive that something regressed between `1b33971` and `5348c2e3` (i.e. in `5ddfde3` or `5348c2e3` itself), but is not conclusive with only one prior successful trial and three current failing ones -- confirming which commit introduced it would need the previous artifact re-served side by side, which this pass did not have the budget for. `introducedByCandidate` and `regressionVsLive` are recorded as **unknown** for this finding. Severity is **major** (CHK-024's own category is `delivery`/`prep`): an annoyance every session a player changes a setting, not a crash, lock, or loss of chapter progress.

**Tooling limitation -- custom-viewport capture.** Setting a custom viewport (1600x900, and separately 390x844) on this shared Browser-pane tab reproducibly rendered only a small top-left fraction of the page in screenshots, while `window.innerWidth`/`innerHeight` and the game's own canvas `getBoundingClientRect()` correctly reported the full emulated size throughout, and DOM scroll-container measurements (used in place of a screenshot for the 390x844 OPTIONS-fit check) were unaffected. This matches the same pane-capture limitation already recorded in `critic/reviews/1b33971-live.md`; screenshots taken at the pane's native/preset size rendered every scene correctly. Not a live-site defect.

**No code was changed, and nothing was committed, pushed or deployed by this review.**
