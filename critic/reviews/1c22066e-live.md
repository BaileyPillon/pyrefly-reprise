```text
Build / artifact / target version: main 1c22066e, bundle l_HXObKO, artifact 9aa052b823fdb68eb990a67d63d4c94d0d28c7927fddef99ed61b416c1ff9e6d
Review: live
Deployment: PASS
Changed area: NOT APPLICABLE (this pass is exact-artifact + live smoke only)
Milestone: not assessed
Quality: not assessed in this pass (see critic/rounds for the last full score)
Targets: not assessed in this pass
Top issues: none crash- or regression-level found; one coverage gap noted below (Chapter V's Node markers at aim were not captured cleanly — a mid-fight character-banter line intercepted the real ATTACK key before the target reticle appeared, so the marker layout at the top edge is UNVERIFIED this pass, not FAIL)
Coverage: tested (see below); reused: none; not tested: full CHK-024 upgrade matrix, physical controller, Safari, Chapter V Node-marker top-edge framing (see above), CHAPTER tab of the phone pause for chapters with more rows
Next required review and why: the deep review already owed on this build and its ten carried-forward builds (per critic/pending/1c22066e.json obligation `deep`, `carriedFrom` fd0ae96 through a999d133); this live pass settles only the `live` obligation
Elapsed review time / repeated work avoided: ~50 minutes hands-on (real-input flows for two games plus a phone-viewport pass, each from a fresh browser context per the task's requirement; no evidence from a999d133-live was reused because the changed systems this build carries include the FFX CTB engine, the battle presenter and the chapter registry)
```

## 1. Exact artifact (CHK-017)

The marker `critic/pending/1c22066e.json` names `"review": "deep"`, so `--full` was used.

`node tools/artifact-manifest.mjs verify-live --manifest critic/artifacts/1c22066e.json --url https://baileypillon.github.io/pyrefly-reprise/ --changed-from critic/artifacts/a999d133.json --full`:

```json
{
 "result": "PASS",
 "artifactHash": "9aa052b823fdb68eb990a67d63d4c94d0d28c7927fddef99ed61b416c1ff9e6d",
 "liveManifest": "match",
 "checked": 942,
 "mismatched": [],
 "missing": [],
 "wrongType": [],
 "errors": [],
 "notes": []
}
```

942 shipped files checked (the full manifest, since a deep review is owed on this build), all byte-identical with the right content types. The hash matches `critic/pending/1c22066e.json`'s `artifactHash` and the deploy log's `bundle=l_HXObKO`. **CHK-017: PASS.**

A first pass without `--changed-from`'s companion (an earlier `--full`-less run) also came back PASS at 52 files (the delta-only set); the full run superseded it and is the evidence of record.

## 2. Real-input smoke (CHK-016)

Browser: `PYREFLY_BROWSER=gpu` Playwright, confirmed via `WEBGL_debug_renderer_info`:
`ANGLE (NVIDIA, NVIDIA GeForce RTX 5070 Ti (0x00002C05) Direct3D11 vs_5_0 ps_5_0, D3D11)` — a real GPU renderer, not SwiftShader. No black-canvas fallback was needed for the desktop passes; the first phone-viewport attempt did produce a black frame (see §4), which was diagnosed as a harness timing issue (a first-time coach toast still claiming the keyboard) and cleared on retry, not a GPU fault.

Every claim below was gated on `window.__pyrefly.screen()` / `battleState()` / DOM text (e.g. waiting for `"ATTACK"` to appear) or `app.save.data` before a screenshot or an assertion; no capture followed a raw timeout.

**FFX real flow 1 — Chapter I (Seymour Flux), real keys from the title, fresh context, 1600x900:**
- Title → real `Enter` → briefing/cutscene → chapter-select (confirmed `screen() === 'chapter-select'`).
- Chapter-select cards observed: `"Chapter I Seymour Flux"` (playable) and `"Seymour and Anima Coming"` (Chapter VII, still a locked `.fe-card--coming` tile — **item 5 confirmed**: Chapter VII is still COMING).
- Entered Chapter I; held real `Enter` (the documented "Hold Enter: skip" cutscene control) to clear the opening scene into `battle`.
- First player turn: the NEXT BEST MOVE (move-advisor) card rendered populated content (`"Next best move · Tidus · Hastega → the party · Guide's pick · in White Magic · 30 MP · always hits + Haste …"`), screenshot `ch1-battle.png`.
- Real `Enter` confirmed ATTACK, opening real target-selection brackets on both enemies (Mortiorchis and Seymour Flux). Screenshot `ch1-target-final.png` shows the corner-bracket target reticles sitting well below/clear of the NEXT BEST MOVE card, with no overlap — **item 1 confirmed**: the target bracket clears the NEXT BEST MOVE card. Real `Escape` cancelled back to the command menu.
- Zero console errors, zero >=400 responses across this context.

**FFX real flow 2 — Chapter VIII (Evrae, airship deck), real keys from the title, fresh context:**
- Title → chapter-select → real chapter selection → held real `Enter` through the cutscene into `battle`.
- First command menu reached (`ORDERS / ATTACK / SPECIAL / WHITE MAGIC / ITEMS / FLEE`). Screenshot `ch8-first-menu.png` shows Rikku standing well clear of the menu's left edge, not overlapped by any menu row — **item 2 confirmed**: Rikku is clear of the command menu at the first menu.
- Zero console errors, zero >=400 responses across this context.

**FFX-2 real flow — Chapter V (Vegnagun/Shuyin), real keys from the title, earlier links skipped only by the documented debug speed-up:**
- Title → chapter-select → real chapter selection → held real `Enter` through the opening cutscene into `battle` (party prep was skippable by default; the prep screen itself was seen once mid-run when a race in the harness pressed `Enter` before the screen had advanced — not a product defect, see §4 notes).
- **DEBUG SPEED-UP (labelled):** `window.__pyrefly.autoBattle('intended')` (documented in `docs/DEV.md` "Debug API") was used to clear link 1 and reach the fight against Vegnagun proper, rather than playing link 1 by hand — this is the shortcut the task permits for "earlier links only."
- Reaching a stable read of `battleState().link === 2` timed out in this pass (the link/phase field name used by the harness did not match what this build exposes, or link 2 was not yet reached in the run's time budget); the run did clearly show real mid-fight state (Vegnagun's HP bar depleted from 4000, Yuna taking a `Tail Beam` hit, Paine at a low-HP banter line), so the fight was progressing, but the harness cannot certify "link 2" specifically without a confirmed state read.
- A real `Enter` confirming ATTACK to bring up the aiming reticle and the Node markers (`.ffx2-nodemark` elements, `nodeEdgeMarkers.ts`) was attempted, but a character-banter line (Paine, "Sphere Hunter") was active at that moment and intercepted the key, so no Node marker was captured on screen this pass.
- **Item 3 (Node markers at the top edge when aiming): UNVERIFIED**, not FAIL — the harness did not reach the intended state cleanly within the time budget, and this is recorded honestly rather than guessed.
- Zero console errors, zero >=400 responses across this context.

**Phone pause (390x844):**
- Real flow into Chapter I's battle (with the first-time Auron coach toast dismissed by a real `Enter`, since it was found to claim the keyboard until dismissed — the first attempt's `Escape`/`P` presses before dismissing the toast produced no visible change, which is what surfaced this).
- Real `P` press (the documented pause key; `KeyP` in `BattleScreen.ts`) opened the phone pause overlay. Screenshot `phone-pause-2.png` shows the Tidus tab: portrait, battle-stats column (HP/MP/Strength/Magic/Defence, Overdrive/Mode/Turn order) sitting well above the chapter caption band ("CHAPTER I · MT. GAGAZET …" / the objective line), with a clear gap and no overlap — **item 4 confirmed** on the Tidus tab.
- The CHAPTER tab specifically (named in `phoneFit.ts`'s own R13-05 note as a location that previously overran) was not re-checked this pass; recorded under `notTested`.
- Real `H` was not re-exercised in this build's smoke pass beyond the prior build's evidence; recorded under `reused: none` (this build touched `pause-phone.css`, a shared system, so prior H-hide evidence was not reused without re-confirming — H toggling on the phone frame is itself in `notTested` this pass; item 4's caption-clearance claim stands on the Tidus-tab screenshot alone).

**Audio:** not independently re-verified via `audioDebug()` in this pass (the debug surface was queried on the desktop Chapter I/VIII contexts implicitly by reaching real battle turns with audio already unlocked by the first real keypress; no explicit `source: 'prerendered'` read was captured this pass — recorded under `notTested`, since the immediately preceding build (`a999d133-live.json`) already confirmed prerendered (not synth) playback on the same shared audio plumbing and no audio-affecting path is in this build's changed-file list).

**Console / network, whole run:** 0 console errors, 0 responses ≥400, across every context (Chapter I, Chapter VIII, Chapter V, phone).

## 3. Reload smoke (CHK-024, lightweight)

A real save-backed setting (`Settings.masterVolume`) was set to `0.37` via the live save object, then the page was fully reloaded. After reload, `masterVolume` read back `0.37` — the setting survived. The full upgrade matrix (fresh / returning / migrating-save / invalid-storage / reset) is **not tested** in this pass, as scoped.

## Coverage

**Tested:** artifact-manifest verify-live, full set (942 files, byte-identical); Chapter VII still a locked COMING card; Chapter I real flow to a player turn, NEXT BEST MOVE card populated, target bracket clear of that card on a real ATTACK confirm; Chapter VIII real flow to the first command menu, Rikku clear of it; Chapter V real flow into the fight with the documented `autoBattle` speed-up past link 1; phone pause opened with a real `P` press, Tidus tab's stat column clear of the chapter caption; a real save-backed setting surviving a full reload; console errors and ≥400 responses for the whole run.

**Reused:** none (this build's changed-file list includes the FFX CTB engine, the battle presenter/stage, the chapter registry and `pause-phone.css`, all shared systems, so no evidence was carried forward from `a999d133-live.json` without re-confirming).

**Not tested:** the full CHK-024 upgrade matrix; a physical controller; Safari; Chapter V's Node markers specifically at the top edge while aiming (a banter line intercepted the attempt — see §2); the phone pause's CHAPTER tab; an explicit `audioDebug()` read confirming prerendered (not synth) audio on this exact build (no audio-affecting file is in this build's change set, so this is a low-risk gap, not disclosed as a defect).

## Verdict

**Deployment: PASS.** The live URL serves the exact 1c22066e/l_HXObKO artifact (942/942 files byte-identical), and every real-input flow that was driven to completion behaved correctly with zero console errors and zero failed network requests. One item (Chapter V's Node markers at aim) could not be captured cleanly within this pass's time budget and is recorded as UNVERIFIED rather than guessed at; nothing observed suggests it is broken, but it is not proof either way.
