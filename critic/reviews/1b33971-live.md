Build / artifact / target version: main 1b33971, bundle BvhtVfzJ, artifact f29a55dfea95b61e35a26c0a676790d40bf778a90937361de1976271350621bf
Review: live
Deployment: PASS
Changed area: NOT APPLICABLE
Ship: NOT APPLICABLE (live review does not carry a ship verdict; changed-area/ship were the focused report's job)
Milestone: not assessed
Quality: not assessed by this review (see critic/reviews/1b339718e023d4dd8a5868901d55ba000a9e59a9-focused.json for the last full/focused score)
Targets: not assessed by this review
Top issues: none found in the checked scope
Coverage: tested — CHK-017 exact-artifact verify-live (863 files, 0 mismatched/missing/wrong-type/errors, --changed-from 8f48237, --full); CHK-016 real-input smoke (title → Auron's-briefing onboarding → chapter select → party prep → cutscene → Chapter I battle, screen/chapter/state asserted via window.__pyrefly.screen() and the on-screen "Chapter I · Mt. Gagazet — The Prominence" header at each step); pause-remake changed flow (Pause chip → tab strip Tidus/Yuna/Kimahri with live per-character Battle Stats meters matching the in-battle HUD values, Music tab with the sound-test track list and prerendered-vs-fallback confirmed via audioDebug(), H hide/show panels, Esc/Resume back to battle); prerendered-music-vs-fallback (network log shows real .mp3 fetches for title/chapter-select/scene-gagazet/boss-seymour/pause/boss-ffx2-aeon, and audioDebug() reports the pause track as cached:true, source:"prerendered"); CHK-024 lightweight reload smoke (Master Volume slider real-click 80→90, page reload, audioDebug().volumes.master === 0.9 after reload; save file also carried seymour-flux attempts:2 and ffx2Atb:"active" across the reload); console error / 404 count for the run.
Coverage: reused — none (this is a fresh live check; the stale critic/reviews/1b33971-live.{md,json} written before the deploy records existed is superseded by this report).
Coverage: not tested — the full reload/upgrade matrix (only one setting + one piece of attempt progress were exercised, not every save field or every screen); FFX-2 chapters/Active ATB were not separately smoke-tested this pass (only confirmed present as a setting, ffx2Atb:"active", in the save file); the required 1600×900 focused-layout capture could not be completed cleanly — see note below; a full playthrough to a win/loss outcome was not run (out of scope for a live/deployment check per RUBRIC §4's "Live" row).
Next required review and why: the deep review of every affected chapter and system is still owed on this build per critic/pending/1b33971.json (carried from fd0ae96/8f48237); this live pass settles only the `live` obligation.
Elapsed review time / repeated work avoided: ~35 minutes (extended past the 2-5 minute small-change budget by browser/viewport friction described below; the CHK-017 manifest diff and the console/network evidence were reused directly rather than re-derived by hand).

## Notes

**CHK-017 — exact artifact.** `node tools/artifact-manifest.mjs verify-live --manifest critic/artifacts/1b33971.json --url https://baileypillon.github.io/pyrefly-reprise/ --changed-from critic/artifacts/8f48237.json --full` returned:
```
{"result":"PASS","artifactHash":"f29a55dfea95b61e35a26c0a676790d40bf778a90937361de1976271350621bf","liveManifest":"match","checked":863,"mismatched":[],"missing":[],"wrongType":[],"errors":[],"notes":[]}
```
The hash matches `critic/artifacts/1b33971.json` and `docs/deploys.log`'s last line. This is a genuine PASS, not UNVERIFIED: the manifest file exists and every one of the 863 shipped files (bundle, CSS, fonts, art, audio) hashed identical to the live bytes.

**CHK-016 — real-input smoke.** Real key/click input only (Return, Escape, real clicks and drags; no debug `goto`/`gotoChapter` was used to reach any of the states below). Sequence and asserted state at each step:
- Title → pressed Return → **Auron's-briefing onboarding cutscene** appeared (the "Five fights..." card), confirming onboarding is switched on as the release notes state; advanced with Return/Esc.
- Chapter select confirmed by the on-screen "CHAPTER SELECT" header and the FFX/FFX-2 lists; **Seymour and Anima**, **Evrae** and **The Leblanc Syndicate** all showed the "Coming" lock, matching "the three new chapters stay locked as Coming."
- Selected Chapter I (Seymour Flux, CTB tag) → Party Prep screen confirmed by header "I · Seymour Flux · Mt. Gagazet · The Prominence" and the three-member roster (Tidus/Yuna/Kimahri) with live HP/MP.
- Started Battle → pre-battle cutscene confirmed by header "Chapter I · Mt. Gagazet — The Prominence" and named speaker portraits (Kimahri, Seymour, Tidus, Auron, Yuna, Lulu) advancing through dialogue with real clicks (the in-game typewriter/advance control, not a skip API).
- Battle screen reached: `window.__pyrefly.screen()` returned `"battle"`; HUD showed Tidus/Yuna/Kimahri stat bars, an enemy read-out for "Seymour Flux", the move-advisor "Watch"/"Rules" panels (move advisor v2, FFX-only — this chapter is CTB/FFX) with sourced citations (`ffx-seymour-flux §…`), and Turn order "1st of 5" / "5th of 5" as different party members were viewed.
- Pause chip → clicked → `window.__pyrefly.screen()` returned `"pause"`; pause screen matched the release description: one painted close-up per member (Tidus/Yuna/Kimahri tabs), live Battle Stats meters that matched the in-battle HP/MP/Overdrive values exactly (e.g. Yuna 1500/1500 HP, 270/270 MP, Overdrive 40% in both the HUD and the pause screen), Chapter/Guide/Options/Controls/Music tabs, "IN THIS FIGHT" panel with Mode/Turn order/the current advisor tip.
- Music tab: sound-test track list with a STOP control and 21 named tracks; clicking a track played it.
- H → panels hidden (full-bleed painted close-up, footer read "H SHOW PANELS · ESC RESUME"); H again → panels restored. Both real clicks.
- Esc / Resume → returned to `screen() === "battle"` with the scene fully intact (party and Mortiorchis positions matched pre-pause).

**Prerendered audio vs. fallback.** Network log shows real `.mp3` GETs (`audio/music/title.mp3`, `chapter-select.mp3`, `scene-gagazet.mp3`, `boss-seymour.mp3`, `pause.mp3`, `boss-ffx2-aeon.mp3`) all 200, and `window.__pyrefly.audioDebug()` reports `playing: "pause"` with that track's entry `cached: true, source: "prerendered"` — this is prerendered audio actually playing, not the procedural synth fallback (a fallback track would show `source: null` and no prior fetch, as the *other*, not-yet-played tracks correctly do).

**CHK-024 (lightweight reload smoke).** On the Options tab, clicked the Master Volume slider with a real click (moved 80 → 90). Reloaded the page (fresh navigation with a new cache-busting query, not a soft reset). After reload: `audioDebug().volumes.master === 0.9` (the change survived), and `localStorage['pyrefly-reprise:save:v1']` showed `settings.masterVolume: 0.9` alongside `chapters.seymour-flux.attempts: 2` — the attempt count from the battle entered during this session also survived the reload, so both the changed setting and a real piece of progress persisted. The full upgrade matrix (every setting, a cleared chapter, unlock state) was **not** tested — out of scope for this lightweight pass per the task.

**Console errors / 404s.** Zero console errors across the whole run (`read_console_messages` with a broad filter returned only `[info]`/`[warn]` lines: a chapter-select dev note, a Canvas2D `willReadFrequently` perf hint, a THREE.js `PCFSoftShadowMap` deprecation note, and one `[painted]` warning that `art/characters/seymour-flux-body/cast.png` had an opaque studio background cleaned at load time — pre-existing art-pipeline warnings, not related to this release's changes). Zero failed/zero-byte network responses (`performance.getEntriesByType('resource')` found none with `transferSize === 0`); every request observed in `read_network_requests` returned 200. This matches the release note's disclosed majors (FOC-05 coach-mark overlap, FOC-06 advisor-card type size) being visual/UX issues, not console or network failures — neither was specifically re-measured in this pass (out of scope for CHK-016/017/024; they are the focused report's obligation, already disclosed there).

**Tooling limitation — 1600×900 capture.** The task calls for "1600x900" real-input smoke. Setting a custom 1600×900 viewport emulation on this shared Browser-pane tab reproducibly rendered only a small top-left fraction of the page in every screenshot (confirmed on the title screen, chapter select and the pause screen — same artifact every time), while `window.innerWidth/innerHeight` and the game's own canvas/`getBoundingClientRect()` correctly reported 1600×900 throughout. This reproduced identically after a full page reload, which rules out a one-off compositor hiccup, and cleared immediately when the viewport was returned to the pane's native/preset size (which rendered every scene correctly at full frame). Nine tabs were open concurrently in this shared browser session (other agents' living-portrait WebGL rig, audio auditions, etc.), consistent with RUBRIC §9's "check GPU contention with art rendering" guidance — a resource-contention/compositor artifact of the review host, not a defect in the shipped page (the DOM/canvas metrics were correct throughout; only the pane's own screenshot capture at that specific emulated size was affected). The smoke test was therefore completed at the pane's native size instead, which is sufficient to assert screen/chapter/state (CHK-016) but not to make a 1600×900 layout/clipping judgement — logged under `notTested`.

**No code was changed, and nothing was committed, pushed or deployed by this review.**
