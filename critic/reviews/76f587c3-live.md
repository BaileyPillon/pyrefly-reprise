```text
Build / artifact / target version: main 76f587c3, bundle 81kxOXnv, artifact b1924762cdc2ab4467d61b6f39c97f26cc37ac1f28e8fd59787ee2824ecd6a2a
Review: live
Deployment: PASS
Changed area: NOT APPLICABLE
Ship: NOT APPLICABLE (already deployed under Bailey's owner override; this review verifies the live artifact, not a ship decision)
Milestone: not assessed
Quality: not assessed by this review (see the deep review still owed on this build per critic/pending/76f587c3.json)
Targets: not assessed by this review
Top issues: none found
Coverage: see below
Next required review and why: the deep review already owed and carried onto this build (per critic/pending/76f587c3.json's obligations), plus a focused review of the changed area, neither of which is this review's job
Elapsed review time / repeated work avoided: ~80 minutes; CHK-017's manifest diff reused the previous build's (d9decadb) manifest rather than re-deriving one
```

## Deployment verification — release 12 (main 76f587c3, bundle 81kxOXnv)

### CHK-017: exact artifact — PASS

`node tools/artifact-manifest.mjs verify-live --manifest critic/artifacts/76f587c3.json --url https://baileypillon.github.io/pyrefly-reprise/ --changed-from critic/artifacts/d9decadb.json` returned:

```json
{"result":"PASS","artifactHash":"b1924762cdc2ab4467d61b6f39c97f26cc37ac1f28e8fd59787ee2824ecd6a2a","liveManifest":"match","checked":64,"mismatched":[],"missing":[],"wrongType":[],"errors":[],"notes":[]}
```

The live site serves exactly this candidate's shipped files. Nothing shown below is a matching bundle name standing in for identity — the manifest was diffed byte-for-byte.

### Real-input smoke — PASS

Ran against `https://baileypillon.github.io/pyrefly-reprise/?livecheck=76f587c3`, 1600x900, fresh browser contexts, `PYREFLY_BROWSER=gpu` (ANGLE D3D11, confirmed `NVIDIA GeForce RTX 5070 Ti` via `UNMASKED_RENDERER_WEBGL`) for the primary sections. Partway through (pause tabs and the Chapter III recheck) the headless Chromium process crashed from GPU contention with ~39 unrelated Chrome windows already open on this shared machine — not a page/product error (0 console errors were logged in every section, including the ones before the crash) — and a **swiftshader fallback** was used once for those two remaining captures, disclosed per the task's own fallback allowance.

1. **Chapter VI (Leblanc):** real double-Enter from title reached chapter select; Chapter VII (Seymour and Anima at Macalania) is confirmed still a locked COMING card (`coming: ["seymour-anima-macalania"]`), Chapter VIII (Evrae) is not in that list. Entering Leblanc with cutscenes live and stepping through with real Enter presses (falling back to `advanceCutscene()` only on lines a bare key press did not advance) reached Brother's FFX-2 comm line with his portrait (`art/portraits/brother-x2.png`) visible in the dialogue card. In battle, Dr. Goon, Fem-Goon and Ormi are painted actors facing the party — **not silhouettes** — confirmed once the stage had settled (`screenState.actors.length` reaches 6 about 3 seconds after `screen()==='battle'`; an earlier capture taken too soon showed an apparently empty stage, which was a harness-timing artifact caught and corrected mid-run, exactly the CHK-016 failure mode, not a product defect). The command help band and the party HP rows sit in visually separate regions with no overlap in that capture (`docs`-equivalent evidence: `13-ch6-after-waits.png` in the run's scratch folder); a DOM-measured bounding-box check was attempted but the selectors tried did not match this build's actual class names, so that specific claim rests on the screenshot rather than a measured rect (see `notTested`).
2. **Chapter V (Vegnagun/Shuyin):** Nooj's dialogue portrait (`art/portraits/nooj.png`) is shown and visible in the dialogue card during the real cutscene.
3. **Pause on FFX-2, Yuna and Paine tabs, 1600x900:** opened live mid-battle with a real Escape (`screen()` → `pause`); real ArrowRight presses reached both tabs. Both screenshots show the stats/meta columns clear of the character's face — the layout mirrors the chrome to whichever side keeps the face clear, matching the framing design read from `src/app/screens/pause/plates.ts`.
4. **Pause hide/restore:** a real `H` press hides all panels (only the "H SHOW PANELS / ESC RESUME" hint remains); a second `H` restores them; a real Escape resumes to `battle`.
5. **Chapter VIII (Evrae):** battle reached victory; every `*/art/characters/*/ko.png` request was logged for the whole run and none named Evrae — only the party's own KO portraits (members downed mid-fight) were requested. This is consistent with `departurePoses()` in `src/engine/BattlePresenterDepartures.ts` mapping Evrae to `'falls-away'`: no KO painting.
6. **Chapter III (Braska's Final Aeon):** cutscenes were *not* skipped this pass, so the run reached the real post-battle aftermath scene (`screen()` → `cutscene`). `audioDebug()` read at that moment: `playing: null`, `music.current: null` — no track, victory or otherwise, is sounding under the aftermath.
7. **Chapter VII:** confirmed locked (see item 1).
8. **Console errors / network 404s:** 0 and 0 across every section of the run.

### CHK-024: reload smoke — PASS (with a caveat)

A seed-7 run of Chapter I (seymour-flux) recorded progress to the save (`attempts: 1`, `playTimeMs: 5016.5`); a real page reload of the live URL produced the byte-identical save blob. **Caveat:** the setting flipped for this pass (`setMuted(true)`) turned out to be a runtime-only audio flag, not part of the save, so its own persistence was not what was exercised — the save's own progress/settings fields are what was proven to survive the reload. A repeat with a save-backed setting (via the pause OPTIONS tab) would be a stronger pass; noted under `notTested` rather than overclaimed.

### Not tested this pass

- Yunalesca and Bahamut were not re-verified live.
- A DOM-measured (rather than visual) check that the help band never overlaps the party rows, and at other resolutions.
- The full save/settings upgrade matrix; a save-backed setting specifically.
- Physical controller and Safari/real-device input.
- Combat correctness/balance beyond reaching a real command menu and letting the fight resolve.
- A mid-fall screenshot of Evrae's actual departure animation (only the KO-asset-never-requested evidence and the design source were checked).

No defects were found in what was tested. `deployment: PASS`.
